# RUN.md — 위키 작성·보강 명령어

이 문서는 근거 기반 한의학 위키를 작성·보강·검증하는 데 쓰는 명령어 모음이다.
모든 데이터는 `https://med.symbolicinfo.com` API에서 가져온다. 작업 시 AGENTS.md의 핵심 원칙(근거 우선, 출처 명시)을 반드시 따른다.

**작업 수행 방식**: opencode는 위키의 누락된 표제어를 스스로 탐색하거나 기존 문서의 보강 필요성을 판단하여 **임의로 주제를 선택해 작업을 수행**할 수 있다.

**독자 가정과 서술 톤**: 독자를 **한의사 임상가**로 가정한다. 따라서 각 근거 기반 서술은 한의학 치료법(침구·처방·본초·추나 등)에 **우호적(호의적) 관점**으로 작성한다. 즉, 근거가 유의하거나 긍정적인 결과를 보인 연구를 강조해 제시하고, 한의학적 접근의 임상적 가치와 적용 가능성을 적극적으로 부각한다. 다만 근거가 부족하거나 부정적인 결과도 숨기지 않고 정직하게 기술하되, 그 해석은 한의학적으로 유의미한 방향(치료 원리·개선 방향·추가 연구 제안)으로 유도한다. 출처 명시는 AGENTS.md 원칙을 그대로 유지한다.

## 공통

```bash
BASE="https://med.symbolicinfo.com"
```

**실행 프롬프트 노출 금지 (강제)**: 위키 문서는 최종 산출물이다. 문서 본문에는 이 워크플로우의 실행 지시(프롬프트)가 어떤 형태로도 노출되어서는 안 된다. 구체적으로 다음을 금지한다:
- "RUN.md 원칙에 따라", "AGENTS.md 규칙에 따라", "프롬프트에 따라" 등 워크플로우·명령어 문서를 언급하는 문구
- 검색·증식·보강·검증·빌드 등 작업 절차나 명령어의 언급
- 근거 정리 방향·서술 톤 등 실행 지시가 그대로 베껴진 문구 (예: "한의학적으로 우호적인 관점에서 근거를 정리한다")

문서는 항상 독자를 위해 근거·임상 정보만 서술한다. 검증 시에도 문서 본문에 프롬프트 혼입이 있는지 확인한다.

## 0. 기준 문서와 작성 목표 (강제)

**모든 신규·보강 문서는 `wiki/기초한의학/한방병리학/간혈허 (肝血虛, Liver Blood Deficiency).md`와 `wiki/기초한의학/한방병리학/간기울결 (肝氣鬱結, Liver Qi Stagnation).md`의 구조·깊이·인용 밀도를 기준(Azure/gold standard)으로 작성한다.** 작성 전 반드시 두 문서 중 하나를 먼저 읽고 동일한 수준의 문서를 만드는 것을 목표로 한다. 두 기준 문서가 공통으로 갖추고 있는 필수 요소는 다음과 같다.

### 0-1. 필수 구조 요소 (기준 문서에서 추출)

| 요소 | 기준 | 비고 |
|---|---|---|
| **다편(多篇) 구조** | 5편 이상의 대편(大編)으로 구성 (예: 총론→병태생리→치법·방약·침구→KCD 질환군별 각론→예후·관리) | 표제어 성격에 따라 편 수는 조정 가능하나 단일 섹션 문서는 금지 |
| **KCD 질환군별 각론** | 질환·증후 문서는 **KCD-8 코드별로 군(群)을 묶어 6단 구성** (①코드 ②병인병기 ③병태생리 ④치료 ⑤예후 ⑥관리) 적용 | 본초·경혈·처방 문서는 이 축을 생략하거나 관련 질환군으로 대체 |
| **규모** | 1,000줄 이상 | 본초·경혈 등 좁은 표제어는 500줄 이상 최소 |
| **인용 수** | 서로 다른 DOI/PMID 100편 이상 (좁은 표제어는 50편 이상) | `per_page=100` 다중 페이지 순회로 전수 확보 |
| **병태생리편** | 전통 병기 + 현대적 상관물(인간·실험 자료) 대응표 | "전통 서술 → 대응 관찰 소견" 형식 |
| **치법·방약·침구편** | 치법 위계 → 대표 방제 방해(方解) → 본초 각론 → 침구 취혈 | 군·신·좌·사 표, 배오 원칙 표, 혈위 배혈표 포함 |
| **감별진단** | 유사 변증 감별표 + 실무 순서 + 기질적 질환 배제 목록 | 변증 간 생물학적 차이 근거(MI RNA·단백체 등)까지 반영 |
| **근거 한계 명시** | 치료표·변증표 뒤에 "이 표는 임상 틀이지 동일 근거수준의 권고가 아니다" 식의 한계 문구 | 모든 표에 부착 |
| **변증 층화 강조** | "변증 없는 관행적 취혈/처방은 근거에 부합하지 않는다" 원칙 반복 명시 | 각 질환군 치료 섹션에 포함 |
| **안전성 항목** | 약물상호작용·부작용·임신 금기·기질질환 배제·정신과 공동 관리 등 명시적 항목 | 예후·관리편에 별도 표 |
| **추적 지표표** | 질환군별 평가 지표(HAM-D, VAS, PSQI, 자궁동맥 PI 등) | 관리 섹션 |
| **환자 설명용 요약** | 일반인 향후 단락 ("간혈허는 …입니다") | 관리편 말미 |
| **Q&A** | 임상 현장 자주 묻는 질문 5~8문항 | 문서 끝 (각주 정의 전) |
| **조섭표** | 오로칠상·생활지도 표 (수면·눈사용·활동·정서·식이·보온·방로 등) | 관리편 |
| **교차 참조** | 관련 변증·방제·본초·경혈 문서 상대 링크 | 본문 최초 등장 시 1회 |
| **각주 정의 부연** | 단순 메타데이터 너머 근거의 의미를 한 줄로 요약 (예: "정지-자율신경 축의 직접 증거") | 말미 각주 정의 |
| **고전 인용 출처** | 『내경』·『상한론』·『금궤』 등 원전 출처 명시 | 문서 맨 끝 |

### 0-2. 표제어 유형별 구조 가이드 (고정 템플릿 아님)

기준 문서 두 편은 **증후(병리)** 표제어다. 표제어 유형에 따라 다편(多篇) 구성을 다르게 설계한다. 아래는 참고용이며, 반드시 이 순서·이름을 따를 필요는 없다.

**① 증후·병리 (간혈허·간기울결 계열)**
- 제1편 총론 (생리 → 병인 → 병기 → 증후학 → 감별진단)
- 제2편 병태생리 (현대적 상관물 → 전통 이론 대응 → 방제 작용 경로 → 변증 간 생물학적 차이)
- 제3편 치법·방약·침구 (치법 위계 → 대표 방제 방해 → 본초 각론 → 침구)
- 제4편 KCD 질환군별 각론 (각 군 6단 구성)
- 제5편 예후와 관리 (예후 인자 → 회복 경과 → 관리원칙 → 조섭 → 환자 설명 → Q&A)

**② 질환 (요통·두통·불면증 등)**
- 제1편 개요·역학·KCD-8 코드·양방 진단 기준
- 제2편 병인병기·병태생리 (한의학 병기 + 양방 병태생리)
- 제3편 변증 (주요 변증별 주증·설맥·치법·대표방) + 감별
- 제4편 치료 (침구·방약·본초·추나·외치 등) — 근거 수준별 분류
- 제5편 예후·관리·환자교육·추적지표
- 제6편 Q&A

**③ 처방·방제 (보중익기탕·소요산 등)**
- 제1편 정의·출전·구성(군신좌사)·방의(方義)
- 제2편 주치·변증 적응 (어떤 변증에 쓰는가, 감별 처방)
- 제3편 가감법(가감) — 병기·겸증별
- 제4편 본초 각론 (구성 약물별 약리·배오 의미)
- 제5편 임상 근거 (질환별·연구 유형별)
- 제6편 약동학·약물상호작용·안전성
- 제7편 Q&A

**④ 본초 (인삼·당귀·황기 등)**
- 제1편 개요·성미귀경·효능·포제
- 제2편 약리 작용 (현대 연구: 조혈·면역·대사·신경 등 축별)
- 제3편 배오(配伍) 원리 — 대표 방제에서의 역할
- 제4편 임상 적용 (질환·증후별 근거)
- 제5편 용량·용법·안전성·약물상호작용
- 제6편 품종·포제·품질 근거
- 제7편 Q&A

**⑤ 경혈 (합곡·태충·삼음교 등)**
- 제1편 위치·유래·소속경락·혈성
- 제2편 주치 (전통 주치 + 현대 임상 적용)
- 제3편 침구법 (자침 깊이·보사·구법·전침)
- 제4편 배혈(配穴) — 증상·질환별 가혈
- 제5편 임상 근거 (질환별 RCT·메타분석)
- 제6편 해부학·신경지배·작용 기전
- 제7편 Q&A

## 1. 위키 생성 (신규 문서 작성)

새 문서는 주제 선정 → 중복 검증 → 논문 수집 → 목차 스켈레톤 → 헤더별 증분 작성 → 검증 순서로 진행한다.

### 1-0. 새 표제어 주제 선정 및 중복 검증

새로 작성할 표제어(본초·경혈·처방·질환·용어 등)를 선정하고, 해당 문서가 이미 존재하는지 철저히 검증한다.

```bash
# 1. 기존 파일 목록 확인 (예: 본초학 폴더)
ls -R wiki/기초한의학/본초학/

# 2. 선정 후보의 중복 여부 전수 검사 (파일명에 키워드가 포함된 모든 파일 검색)
glob "wiki/**/천마*.md"
```

**검증 규칙**:
- `glob` 결과가 0건일 때만 신규 작성을 진행한다.
- 특정 폴더(예: 방제학)에 파일이 있더라도, 대상 분류(예: 본초학)에 해당 파일이 없다면 신규 작성이 가능하다. 단, 동일한 성격의 문서가 이미 존재하는지 반드시 확인한다.

**주제 선정 우선순위**:
1. 기존 문서에서 교차 참조로 링크된 미작성 문서 (끊어진 링크 우선)
2. 한의과대학 교과과정 핵심 표제어 중 미작성 항목
3. 기존 문서 보강이 필요한 경우 (근거 수·구조·표점 오류 등)

### 1-1. 기준 문서 숙독 (강제)

신규 작성 전 반드시 다음 두 기준 문서 중 하나를 완독한다:

```bash
# 증후·병리 표제어인 경우
read wiki/기초한의학/한방병리학/간혈허\ \(肝血虛,\ Liver\ Blood\ Deficiency\).md
read wiki/기초한의학/한방병리학/간기울결\ \(肝氣鬱結,\ Liver\ Qi\ Stagnation\).md
```

기준 문서의 다편 구조·인용 밀도·근거 한계 명시 방식·Q&A 형식·조섭표·환자 설명 단락·각주 부연 해석을 동일하게 적용하는 것을 목표로 한다.

### 1-2. 관련 논문 전수 수집 (포괄적 검색어 확장)

**포괄적 검색어 확장(강제)**: 단일 검색어에 그치지 않고 주제의 한글명·영문명·동의어·관련 증상·관련 처방·관련 경혈·관련 한의학 변증 용어 등을 조합해 여러 검색어로 반복 검색하고, `cat`(연구 유형)·`km`·`human`·`lit`·`source` 필터를 다양하게 조합해 임상시험·체계적 고찰·메타분석·관찰연구·증례 등 다양한 연구 유형의 논문을 확보한다.

```bash
# 주제별 논문 검색 (analyzed=1 우선 사용, per_page=100으로 전수 수집)
curl -s "$BASE/search?q=검색어&km=1&human=1&analyzed=1&per_page=100"

# 연구 유형별 분리 검색
curl -s "$BASE/search?q=검색어&cat=clinical_trial&analyzed=1&per_page=100"
curl -s "$BASE/search?q=검색어&cat=systematic_review&analyzed=1&per_page=100"
curl -s "$BASE/search?q=검색어&cat=meta_analysis&analyzed=1&per_page=100"
curl -s "$BASE/search?q=검색어&cat=case_report&analyzed=1&per_page=100"

# 영문 동의어 검색 (PubMed/Crossref 소스)
curl -s "$BASE/search?q=low back pain&source=pubmed&analyzed=1&per_page=100"

# scripts/search.py로 목표 수량 확보 (DOI 중복 제거, 다중 페이지 순회)
python3 scripts/search.py "요통" --km --human --analyzed --per-page 100 --target 50
```

**연구 유형 우선 + 임상 참고 논문 필수 인용(강제)**: 최대한 높은 연구 유형(체계적 고찰/메타분석 > 임상시험 > 관찰연구 > 증례)의 논문을 우선 선별한다. 다만 낮은 연구 유형이라도 임상에 참고가 되는 논문(관찰연구·증례·파일럿 등)은 반드시 인용한다. **검색으로 확보한 관련 논문을 임의로 버리지 않고**, 높은 연구 유형을 우선하되 임상적 가치가 있는 논문은 빠짐없이 본문(각주)에 반영한다.

**검색된 모든 논문 전수 분석·인용 규칙(강제)**: `per_page=100`으로 검색하고 필요 시 여러 페이지를 순회해 후보를 전수 수집한 뒤, **같은 DOI/PMID의 논문은 하나로 병합(중복 제거)하고, 그 결과물인 모든 논문을 본문(각주)에 빠짐없이 반영**한다. 단순히 최소 수량만 채우는 데 그치지 않고, 검색으로 확보된 논문 중 주제와 관련된 것은 하나도 누락하지 않도록 한다. 검색 결과가 많아 문서가 길어질 경우에도, 논문을 임의로 버리지 말고 하위 표제어·변증·치료법별로 나누어 모두 수록한다.

**최소 인용 논문 수(강제)**:
- 증후·병리·질환 표제어: 서로 다른 DOI/PMID **100편 이상** (기준 문서 수준)
- 처방·방제 표제어: 50편 이상
- 본초·경혈 표제어: 50편 이상 (해당 본초·경혈이 포함된 모든 연구)
- 기타 용어: 30편 이상

만약 검색으로도 목표 수의 논문만 확보되는 주제라면, 그 사실과 확보한 논문 수를 문서에 명시하고 부족한 이유를 정직하게 기술한다.

### 1-3. 문서 파일 작성 — 2단계 방식 (강제)

신규 문서 작성·기존 문서 대규모 개편 시 다음 **2단계 방식**으로 진행한다. 한 번에 완성본을 쓰지 않는다.

#### 1-3-1. 폴더 구조 생성

```bash
# 위키 폴더 구조 (필요한 폴더만 생성)
mkdir -p wiki/기초한의학/{원전,진단학,한방생리학,한방병리학,경락경혈학,침구학,본초학,방제학,기공학,의사학} \
         wiki/임상한의학/내과/{비계,폐계,심계,간계,신계} \
         wiki/임상한의학/{외과,산부인과,소아과,신경정신과,안이비인후피부과,침구과,추나의학,재활의학,사상의학} \
         wiki/보건기타/예방한의학
```

**분류 단계 유연성**: 위키 분류는 대분류→중분류→소분류의 3단계에 얽매일 필요가 없다. **필요에 따라 더 하위로 분류를 뻗어나갈 수 있다.** 소분류 아래에도 4단계·5단계 이상의 하위 폴더를 두어 상세하게 분류할 수 있고, 반대로 어떤 주제는 2단계로 구성해도 된다. 폴더를 새로 만들면 그 하위 구조에 맞춰 각 상위 폴더의 `README.md` 목차를 갱신한다.

#### 1-3-2. 1단계 — 목차(헤더) 스켈레톤 생성

문서 파일을 만들되, **다편(多篇) 구조의 소제목 헤더(목차)와 각주 마커(`[^n]:`) 정의 자리만 먼저 배치**한다. 본문 서술 내용은 채우지 않고, 제목·소제목 구조만 세운다.

**목차 설계 원칙**:
- 0-1의 필수 구조 요소를 모두 포함하는 다편 구조로 설계한다.
- 표제어 유형(증상·질환·처방·본초·경혈)에 따라 0-2의 가이드를 참고해 독자적 구성을 만든다.
- 질환·증후 표제어는 **KCD 질환군별 각론 편**을 반드시 포함한다.
- 각 소제목 아래에는 `(작성 예정)` 같은 임시 표기 대신 빈 칸을 둔다.
- 각주 마커 자리만 확보해 둔다 (번호는 2단계에서 확정).

#### 1-3-3. 2단계 — 헤더별 근거 검색 + diff 채우기

1단계에서 세운 목차의 **헤더를 하나씩 선택해**, 해당 헤더에 맞는 근거를 검색 API(`/search`)로 조회한 뒤 **diff(증분 편집) 방식으로 해당 헤더의 내용만 채운다.** 모든 헤더를 한 번에 채우지 않고, 한 헤더의 서술과 그에 연결된 각주 정의를 완성한 뒤 다음 헤더로 넘어간다.

```bash
# 2단계 예시: "병태생리" 헤더 채우기를 위한 근거 검색
curl -s "$BASE/search?q=<병태생리 관련 검색어>&km=1&human=1&analyzed=1&per_page=100"

# "KCD 질환군별 각론 - 조혈·빈혈군" 헤더 채우기
curl -s "$BASE/search?q=iron deficiency anemia&analyzed=1&per_page=100"
curl -s "$BASE/search?q=철결핍빈혈&km=1&human=1&analyzed=1&per_page=100"
```

**각 헤더 작성 시 준수 사항**:
- 해당 헤더의 서술이 끝나면 `git diff --stat`로 변경 규모를 확인한다.
- 근거가 부족한 헤더는 검색어를 확장해 재검색한다.
- 표(표)를 포함하는 헤더는 표 뒤에 **근거 한계 명시 문구**를 부착한다 (예: "이 표는 임상 틀이지 동일 근거수준의 권고가 아니다").
- KCD 질환군별 각론의 각 군은 **6단 구성** (①코드 ②병인병기 ③병태생리 ④치료 ⑤예후 ⑥관리)을 따른다.

모든 헤더를 순서대로 채운 뒤에야 3-1(검증·빌드)과 커밋 규칙을 따른다.

### 1-4. 문서 서술 규칙 (강제)

#### 1-4-1. DOI/PMID 링크 처리
문서에 등장하는 모든 DOI와 PMID는 반드시 링크로 처리한다.
- DOI: `https://doi.org/<DOI>` (예: `[10.1000/example](https://doi.org/10.1000/example)`)
- PMID: `https://pubmed.ncbi.nlm.nih.gov/<PMID>/` (예: `[PMID 123456](https://pubmed.ncbi.nlm.nih.gov/123456/)`)
- 각주 정의·본문의 모든 DOI/PMID에 적용한다.

#### 1-4-2. 문장별 각주(출처) 처리
본문의 근거 기반 서술은 **문장 단위로 각주(footnote)를 달아 출처를 명시**한다.
- 각 근거 문장(임상적 주장·효능·효과·수치 등)의 끝에 `[^1]`, `[^2]`와 같은 각주 마커를 붙인다.
- 문서 하단에 해당 각주 정의를 작성한다. 각주 정의에는 해당 근거 논문의 **제목·연구유형·저널·DOI/PMID(링크)**을 기재한다.
- 문장이 여러 근거를 인용하면 각주 안에 여러 출처를 나열한다.
- 상식적인 주장은 자연스럽게 서술하며 별도의 근거 표시를 하지 않는다.

#### 1-4-3. 각주 정의 부연 해석 (강제)
각주 정의에는 단순 메타데이터(제목·저널·DOI)만 적지 않고, **해당 근거가 본문에서 어떤 의미를 갖는지 한 줄로 부연 해석**을 덧붙인다. 기준 문서의 예시:

```
[^1]: A study on plasma norepinephrine and epinephrine levels in TCM liver syndromes. _J Tradit Chin Med_. 1997. [PMID 10437216](https://pubmed.ncbi.nlm.nih.gov/10437216/) — 간실증(간기울결·간양상항·간화상염)이 교감신경 항진(NE·E 상승)과 연관됨. 정지-자율신경 축의 직접 증거.
```

#### 1-4-4. 각주 정의로 출처 정보 통일 (강제)
문서의 출처 정보는 **각주 정의로만** 표기한다. 별도의 근거 표나 `## 참고문헌` 섹션(번호 목록)을 두지 않는다. 단, 기준 문서(`간혈허`)처럼 문서 말미에 **출처 분류별 참고문헌 목록**을 부록 형식으로 둘 수는 있다. 이 경우 본문의 각주와 참고문헌 목록이 중복되지 않도록, 각주가 참고문헌 목록을 대신하는 형태(간기울결 방식) 또는 각주 + 분류 목록(간혈허 방식) 중 하나를 택한다. 각주 정의는 문서 맨 끝에 **별도의 헤더 없이, 불릿 기호(`-`) 없이** 각주 마커(`[^n]:`)로만 나열한다.

#### 1-4-5. 핵심 키워드 내부 링크
문서 본문에서 등장하는 **핵심 키워드(본초·경혈·처방·질환·용어)가 위키 내 다른 문서로 존재하면 상대 링크로 내부 연결**한다. 최초 등장 시 한 번 링크하고 반복 등장은 링크하지 않는다. 링크는 AGENTS.md 교과서적 표준 표기를 기준으로 한다 (예: `[합곡(合谷, LI4)](기초한의학/경락경혈학/합곡(合谷, LI4).md)`, `[보중익기탕(補中益氣湯)](기초한의학/방제학/보중익기탕(補中益氣湯).md)`, `[요통(腰痛)](임상한의학/재활의학/요통.md)`). 존재하지 않는 문서로는 링크하지 않는다(링크 대상 파일이 있는지 반드시 확인).

#### 1-4-6. 문서 표점 규칙 (강제)
문서 본문의 모든 문장 부호는 **한국식 표점**을 사용한다. 특히 중국어 표점인 `。`(모점)을 사용하지 않고 **반드시 `.`(마침표)로 통일**한다. AI 요약(`answer`/`clinical_summary`)이나 중국어 원문에서 `。`가 포함된 문장을 인용·번역할 때도 마침표 `.`로 변환하여 기재한다. 이 규칙은 문서 본문·각주 정의·표·Q&A 등 문서 전체에 적용한다. 문서 작성·보강 완료 후 `。`가 남아있는지 확인하고 일괄 변환한다(`sed -i '' 's/。/./g' 파일.md`).

#### 1-4-7. 한의학 용어 한자 병기
한자로 표기할 수 있는 한의학 용어(본초·경혈·처방·원전·진단·병리·증후·기혈·경락 등)는 본문에서 해당 한글 용어 뒤에 괄호로 한자를 병기한다 (예: `보중익기탕(補中益氣湯)`, `합곡(合谷)`, `음허(陰虛)`, `기혈(氣血)`). 최초 등장 시 반드시 병기하고, 이후 동일 용어는 반복 병기하지 않는다.

#### 1-4-8. 한자 발음 표기 규칙 (강제)
본문에서 한자를 병기할 때 해당 한자(한자어)의 읽기는 **반드시 표준 한국어 한자 발음(국어의 표준 한자음)**으로만 표기한다. 이는 선택 사항이 아니라 **무조건 지켜야 하는 강제 규칙**이다. 중국식 발음(병음·광동어 등)·일본식 발음·지방 발음·영어식 표기는 어떤 경우에도 사용하지 않는다. 예: 十味龍膽花 → **십미용담화**, 痰熱清 → **담열청**, 咳喘平湯 → **해천평탕**, 柴僕湯 → **시복탕**, 金振口服液 → **금진구복액**, 加味玉屛風散 → **가미옥병풍산**, 咳速寧顆粒 → **해속녕과립**. 중국어 병음(예: Kesuning)을 그대로 한글로 옮긴 표기(예: 커수닝)는 금지한다.

#### 1-4-9. 본초·경혈·처방 표준 표기
본초·경혈·처방(방제) 이름은 **한의과대학 교과서적 표준 표기**를 쓴다 (예: 경혈 `합곡(LI4)`, 처방 `보중익기탕`, `소건중탕(小建中湯)`, `축천환(縮泉丸)`, 본초 `인삼`·`백출`). 지방·약어·비표준 표기를 피하고 최초 등장 시 한자·약리학적 표기를 병기한다. 표준 표기가 아닌 오기(예: 小建中湯을 "소견중탕"으로, 縮泉丸을 "수권환"으로 적는 것)는 사용하지 않는다.

#### 1-4-10. 제목·파일명 병기
위키 제목(파일명/문서 제목)은 **한글·영어·한자·외래어(원어)를 병기**할 수 있다 (예: `합곡(合谷, LI4)`, `보중익기탕(補中益氣湯)`). **동음이의/다의어가 필요한 문서는 동음이의 분류로 따로 처리**한다 — 파일명에 구분자를 명시하거나(예: `인삼(人蔘)` vs `인삼(삼, Panax ginseng)`), 괄호 표기로 구분하고 폴더 `README.md` 목차에 항목을 명시한다.

#### 1-4-11. YAML front matter 금지
파일 상단에 YAML front matter를 **넣지 않는다.** 대신 문서 첫 줄에 `# 제목` 헤딩과 함께 메타 정보(출처·작성일)는 본문에 자연스럽게 서술한다.

#### 1-4-12. KCD-8 코드 명시
질환 문서는 **KCD-8 코드를 본문 개요 첫 문단에 명시**한다 (예: `요통 (KCD-8: M54.5)`). 파일명에는 코드를 넣지 않는다. 질환·증후 표제어의 KCD 질환군별 각론 편에서도 각 군마다 KCD 코드표를 둔다.

### 1-5. 하위 표제어(세분화 분류) 목록화 규칙

하나의 표제어가 여러 하위 유형·아형·세부 항목을 포괄하는 경우, 단일 문서로 퉁치지 않고 **하위 표제어를 명시적으로 목록화**한다. 이 규칙은 질환뿐 아니라 **본초·경혈·처방·증후·기타 모든 표제어**에 공통 적용한다.

- **하위 표제어 식별**: 해당 표제어의 하위 유형·아형·변증·부위별·병기별 구분을 조사해 목록화한다 (예: 고혈압 → 본태성/폐성/신성, 두통 → 편두통/긴장성/군발성, 인삼 → 인삼/홍삼/백삼 등).
- **문서 내 목차 구성**: 하위 표제어가 여러 개면 문서 본문에 **"하위 표제어" 또는 "분류" 섹션**을 두어 각 하위 항목을 나열하고, 각 항목에 대해 정의·특징·한의학적 변증·치료 근거를 간략히 서술한다.
- **별도 문서 분리**: 하위 표제어가 독립적으로 충분한 근거와 임상적 중요성을 가지면 **별도 문서로 분리**하고, 상위 문서에서 상대 링크로 연결한다 (예: `고혈압.md` → `본태성고혈압.md`, `폐성고혈압.md`). 분리한 하위 문서는 폴더 `README.md` 목차에 명시한다.
- **동음이의/다의어 구분**: 하위 표제어가 동음이의·다의어로 식별이 필요한 경우 AGENTS.md의 동음이의 분류 규칙(파일명 구분자·괄호 표기)을 적용한다.
- **KCD-8 코드**: 질환 하위 표제어는 각각의 KCD-8 코드를 개요 첫 문단에 명시한다 (예: 본태성 고혈압 `I10`, 폐성 고혈압 `I27.0`).

### 1-6. 질환 문서의 세분화된 목차 구성

질환 문서는 단순 개괄이 아닌, 임상 한의사가 전반적으로 활용할 수 있도록 목차를 세분화해 작성한다. 질환에 따라 다음 항목을 적절히 포괄한다:
- **개요** (정의·역학·KCD-8 코드)
- **병인 병기(病因病機)** — 한의학적 원인·병리 기전
- **병태생리** — 서양의학적 병태생리 및 진단 접근
- **진단(診斷) 및 감별 진단** — 병력 청취(주요 증상·경과·악화 요인), 이학적 검사(시진·촉진·신경학적·정형외과적 검사 등), 영상 검사(X-ray·CT·MRI·초음파 등), 실험실 검사(혈액·소변·객관적 지표), 기능 검사(알고미터·위전도·자율신경 검사 등)를 질환 특성에 맞춰 구체적으로 나열하고, 각 검사의 목적·시행 근거·진단적 의미를 기술한다. 또한 **양방 진단 기준(임상 진단 기준·분류 체계·단계/등급)**을 명시하고, 감별해야 할 유사 질환(red flag·위험 신호 포함)을 구체적으로 나열하여 서술한다. 근거가 있는 검사는 DOI/PMID를 명시하고, 없는 항목은 "교과서적 근거"로 표기한다. **교과서적 임상 진단 이론 수준의 내용은 굳이 근거 등급을 매기지 않는다.** 진단 이론·병기·병태생리 등 한의과대학 교과서에 표준적으로 수록된 이론적 내용은 근거 등급을 매기지 않고 "교과서적 근거"라고만 표기한다.
- **변증(辨證)** — 한의학적 변증 분류(각 변증의 주증·설진·맥상·치법)와 KCD 코드, 변증별 감별
- **치료** — 침구·처방·본초·추나 등 한의학적 치료법 및 임상 근거
- **예후** — 치료 반응 및 경과 예측
- **관리** — 생활 관리, 예방, 재발 방지, 환자 교육
- **Q&A** — 임상 현장 자주 묻는 질문
- **근거 요약·각주 정의** — AGENTS.md 구조 유지

각 섹션은 근거 기반으로 작성하며, 해당 질환의 특성에 맞춰 목차를 유연하게 세분화·조정한다.

임상한의학 각 과(내과·외과·산부인과·소아과·신경정신과·안이비인후피부과·침구과·추나의학·재활의학·사상의학) 밑에는 **교과서적 편제로서 해당 임상 각과에서 다루는 각 질환에 대해 서술**한다.

### 1-7. Q&A 섹션 (강제)

**각 문서에는 반드시 Q&A 파트를 포함한다.** 해당 주제에 대해 한의사 임상가가 실제 진료 현장에서 자주 묻는 질문과 그에 대한 근거 기반 답변을 **5~8개**의 문답(Q) 형식으로 수록한다. 기준 문서의 Q&A 형식을 따른다:

```
**Q1. 간기울결은 우울증·불안장애와 같은가?**

아니다. 간기울결은 한의학적 변증(증후 단위)이고, 우울증·불안장애는 정신과 진단명이다. 증상이 겹치지만, **정신과 진단이 있으면 정신과 주치의 관리를 유지하면서 병용**하는 것이 원칙이다. 한약·침구는 **보조 요법**으로 위치한다 [^3].
```

각 답변에는 해당 근거가 있으면 DOI/PMID를 명시하고, 근거가 없으면 자연스럽게 생략한다. 문답은 임상 적용 관점에서 한의학적으로 우호적인 해석을 담는다.

### 1-8. 환자 설명용 요약 (강제)

관리편 말미에 **환자 향후 설명 단락**을 둔다. 기준 문서의 형식을 따른다:

```
> 간혈허는 "피가 모자라다"는 뜻이지만, 검사상의 빈혈만을 말하는 것이 아닙니다. ...
```

의학 용어를 최소화하고, 환자가 이해할 수 있는 비유와 실생활 지침을 포함한다.

### 1-9. 안전성·약물상호작용 항목 (강제)

예후·관리편에 **안전성 표**를 둔다. 기준 문서의 형식을 따른다:

| 위험 | 내용 | 참고 |
|---|---|---|
| 하수오 간손상 가능성 | ... | [PMID ...] |
| 감초 장기 투여 | 부종·저칼륨혈증·고혈압 | ... |
| 항암제 상호작용 | ... | [PMID ...] |

### 1-10. 추적 지표표 (강제)

관리편에 **질환군별 평가 지표표**를 둔다. 기준 문서의 형식을 따른다:

| 영역 | 추적 지표 |
|---|---|
| 조혈 | 헤모글로빈, 페리틴, TIBC, 트랜스페린 포화도, BMI, 림프구수 |
| 수면 | PSQI, ISI |
| ... | ... |

### 1-11. 조섭표 (강제)

관리편에 **오로칠상·생활지도 표**를 둔다. 기준 문서의 형식을 따른다:

| 항목 | 지도 내용 | 이론적 근거 |
|---|---|---|
| 수면 | 자정 이전 취침. 축시(01–03시) 각성 회피 | 「人臥則血歸於肝」 |
| 눈 사용 | VDT 연속 사용 제한, 20-20-20 규칙 | 「久視傷血」 |
| ... | ... | ... |

### 1-12. 고전 인용 출처 (강제)

문서 맨 끝에 **고전 인용 출처**를 명시한다. 기준 문서의 형식을 따른다:

```
**고전 인용 출처**: 『黃帝內經素問』(五藏生成篇, 痿論, ...), 『靈樞』(...), 『難經』, 『傷寒論』, 『金匱要略』, ...
**문헌 데이터 출처**: [한의학 논문 데이터베이스 (med.symbolicinfo.com)](https://med.symbolicinfo.com) — YYYY-MM-DD 조회 기준
```

## 2. 위키 보강 (기존 문서 개선)

기존 문서에 근거를 추가·갱신하거나 새 논문을 반영한다.

**점진적 증분 편집 원칙 (강제)**: 문서 작업량이 대량인 경우가 많으므로, 한 번에 전체를 재작성하지 않고 **부분적으로 점진적으로 보강**한다. 한 섹션(예: 총론·병인병기·병태생리·진단·치료·예후·관리)씩 순차적으로 편집하고, 각 편집 후 `git diff --stat`로 변경 규모를 확인하며 진행한다.

**보강 방식 — 기존 내용을 보존하고 증분으로 추가(강제)**: 문서를 보강할 때는 기존 파트·서술을 삭제하거나 전체를 다시 쓰는 대신, **기존 부분을 그대로 두고 새로운 목차·소제목을 추가하거나 diff(증분) 방식으로 내용을 보강**한다. 즉, 기존 문서의 구성과 문장은 유지하고, 새로 확보한 논문 근거에 맞는 하위 표제어·변증·치료법·Q&A를 추가하거나 기존 각주에 새 논문을 덧붙이는 방식으로 진행한다. 문서 전체를 재작성하는 듯한 인상을 주는 대규모 교체는 피한다.

**보강에는 오류 수정도 포함(강제)**: 보강은 새 근거를 추가하는 것만이 아니라, **기존 문서에 잘못된 내용(사실·수치·논문 인용 오류·연구 유형 라벨 오류 등)이나 잘못된 포맷(표기 규칙 위반, 각주 형식 오류, 깨진 링크, `mkdocs.yml` `nav` 불일치 등)이 발견되면 이를 정정·수리하는 것도 포함**한다.

**보강 시 기준 문서 대비 평가(강제)**: 보강 전 기존 문서를 0-1의 필수 구조 요소 기준과 비교해 부족한 요소를 식별한다:
- 다편 구조가 아닌 단일 섹션 → 다편 구조로 재설계 (헤더 추가 후 증분 채우기)
- KCD 질환군별 각론 부재 → 질환·증후 표제어인 경우 6단 구성 군 추가
- 인용 수 100편 미만 (증상·질환) → 추가 논문 전수 수집·인용
- 근거 한계 명시 부재 → 각 표 뒤에 한계 문구 부착
- Q&A·환자 설명·안전성 표·추적 지표표·조섭표 부재 → 관리편에 추가
- 각주 부연 해석 부재 → 각주 정의에 한 줄 부연 추가

### 2-1. 문서 재검증 및 근거 갱신

```bash
# 기존 문서의 주제로 최신 논문 재검색
curl -s "$BASE/search?q=문서주제어&analyzed=1&per_page=100"

# 최신 통계 확인
curl -s "$BASE/stat"
```

- 최신 `analyzed_at`/`fetched_at` 기준으로 근거(각주 정의)를 갱신한다.
- 누락된 연구 유형 라벨을 보완한다.
- AI 요약(`answer`/`clinical_summary`)을 재대조해 정확성을 확인한다.
- 특정 문서의 근거가 비어 있으면 `cat` 필터로 더 높은 연구 유형(체계적 고찰/메타분석)을 추가로 수집한다.

### 2-2. 부족한 근거 파악

기존 문서의 각주 수를 카운트하고 기준(증상·질환 100편, 처방·본초·경혈 50편) 대비 부족분을 추가 수집한다.

## 3. 검증 및 품질 점검

### 3-1. RUN.md 워크플로우 자동 실행 (작업 후 반드시)

작업이 끝날 때마다 아래 커맨드로 검증·링크 점검·빌드를 한 번에 실행한다.

```bash
# opencode 커맨드 (검증 + 링크 점검 + 빌드)
/run

# 또는 스크립트 직접 실행
python3 scripts/run.py
python3 scripts/run.py --skip-build   # 빌드 생략
python3 scripts/run.py --health       # API 헬스 체크 포함
```

### 3-2. 자동 검증 스크립트

```bash
# 링크·KCD·표준 표기 검증
python3 scripts/validate.py
```

### 3-3. 링크/문서 구조 점검

```bash
# 교차 참조(상대 링크) 끊어진 곳 확인
rg -o '\]\(\.\./[^)]+\)' wiki/ | sort -u
```

### 3-4. 근거 무결성 점검

- 모든 임상 주장에 DOI/PMID가 있는지 확인
- 비한의학 논문이 섞였는지 `is_korean_medicine` 필드로 확인
- AI 요약을 인용으로 사용하지 않았는지 대조

### 3-5. 기준 문서 대비 품질 점검 (강제)

작성·보강 완료 후 0-1의 필수 구조 요소 기준으로 자가 점검한다:

| 점검 항목 | 기준 | 확인 |
|---|---|---|
| 다편 구조 | 5편 이상 | ☐ |
| KCD 질환군별 각론 (질환·증후) | 6단 구성 군 포함 | ☐ |
| 규모 | 1,000줄 이상 (본초·경혈 500줄) | ☐ |
| 인용 수 | 100편 이상 (본초·경혈·처방 50편) | ☐ |
| 근거 한계 명시 | 모든 표에 한계 문구 | ☐ |
| 변증 층화 강조 | 관행적 취혈/처방 경고 | ☐ |
| 감별진단 | 유사 변증표 + 실무 순서 + 기질질환 배제 | ☐ |
| 안전성 표 | 약물상호작용·부작용·금기 | ☐ |
| 추적 지표표 | 질환군별 평가지표 | ☐ |
| 환자 설명용 요약 | 관리편 말미 | ☐ |
| Q&A | 5~8문항 | ☐ |
| 조섭표 | 생활지도 표 | ☐ |
| 고전 인용 출처 | 문서 맨 끝 | ☐ |
| 교차 참조 | 관련 문서 상대 링크 | ☐ |
| 각주 부연 해석 | 각주 정의에 한 줄 의미 | ☐ |
| 표점 `.` 통일 | `。` 잔류 없음 | ☐ |
| 한자 한국식 발음 | 중국식 발음 혼재 없음 | ☐ |

## 4. 헬스 체크

```bash
# API 정상 여부
curl -s "$BASE/health"
```

## 5. 정적 사이트 빌드

```bash
# 빌드 (site/ 출력)
python3 -m mkdocs build

# 로컬 미리보기
python3 -m mkdocs serve
```

## 6. 위키 자동 증식·보강 (opencode 반복 실행)

`scripts/loop.py`가 opencode를 호출해 RUN.md 워크플로우를 반복 실행한다.
새 표제어를 직접 선정해 근거 기반으로 작성하고, 기존 문서의 오류·근거를 갱신한다.

```bash
# 무한 반복 (Ctrl+C로 중단)
python3 scripts/loop.py

# 20회만 실행
python3 scripts/loop.py --max 20

# 실행 사이 5초 대기
python3 scripts/loop.py --interval 5

# 체크포인트 초기화 (처리 이력 삭제)
python3 scripts/loop.py --reset
```

- 진행 이력은 `scripts/.progress.json`, 로그는 `scripts/loop.log`에 기록된다.
- 연속 3회 실패 시 자동 중단한다.

## 유의사항

- API 과호출 방지를 위해 `per_page`는 최대 100, 필요한 페이지만 요청한다.
- 문서 작성/보강 후 반드시 검증 명령어로 링크·근거 무결성을 점검한다.
- 문서 본문에 실행 프롬프트(워크플로우 지시·명령어·시스템 규칙 문구)가 혼입되지 않았는지 검증한다. 혼입 시 즉시 제거한다.

## 커밋 규칙

- **작업 완료 시 자동으로 커밋한다.** 사용자가 별도로 요청하지 않아도 작업이 끝나면 변경 사항을 커밋한다.
- 커밋 전 `git status`/`git diff`로 의도한 파일만 스테이징하고, 시크릿·불필요 파일은 제외한다.
- 커밋 메시지는 변경 내용을 간결히 요약한다 (예: `docs: 위키 분류 체계 초기화`, `feat: 논문 검색 스크립트 추가`).
- **커밋 후 `git push origin main`을 실행해 원격 저장소에 반영한다.**