# RUN.md — 위키 작성·보강 명령어

이 문서는 근거기반 한의학 위키를 작성·보강·검증하는 데 쓰는 명령어 모음이다.
모든 데이터는 `https://med.symbolicinfo.com` API에서 가져온다. 작업 시 AGENTS.md의 핵심 원칙(근거 우선, 출처 명시, 근거수준 구분)을 반드시 따른다.

**독자 가정과 서술 톤**: 독자를 **한의사 임상가**로 가정한다. 따라서 각 근거 기반 서술은 한의학 치료법(침구·처방·본초·추나 등)에 **우호적(호의적) 관점**으로 작성한다. 즉, 근거가 유의하거나 긍정적인 결과를 보인 연구를 강조해 제시하고, 한의학적 접근의 임상적 가치와 적용 가능성을 적극적으로 부각한다. 다만 근거가 부족하거나 부정적인 결과도 숨기지 않고 정직하게 기술하되, 그 해석은 한의학적으로 유의미한 방향(치료 원리·개선 방향·추가 연구 제안)으로 유도한다. 근거수준 라벨링과 출처 명시는 AGENTS.md 원칙을 그대로 유지한다.

## 공통

```bash
BASE="https://med.symbolicinfo.com"
```

## 1. 위키 생성 (신규 문서 작성)

새 문서는 주제 → 논문 수집 → 문서 작성 순서로 진행한다.

### 1-1. 관련 논문 수집 (분석 완료, 한의학, 인체 연구)

```bash
# 주제별 논문 검색 (analyzed=1 우선 사용)
curl -s "$BASE/search?q=검색어&km=1&human=1&analyzed=1&per_page=20"
```

주요 필터 조합 예시:

```bash
# 요통 관련 임상시험/메타분석
curl -s "$BASE/search?q=요통&cat=clinical_trial,meta_analysis&analyzed=1&per_page=50"
curl -s "$BASE/search?q=low back pain&cat=systematic_review&analyzed=1&per_page=50"

# 특정 처방/경혈 관련
curl -s "$BASE/search?q=보중익기탕&analyzed=1&per_page=50"
curl -s "$BASE/search?q=합곡&analyzed=1&per_page=50"
```

### 1-2. 문서 파일 작성

수집한 논문을 근거 표로 정리하고 다음 명령으로 문서를 생성한다.

```bash
# wiki/질환/요통.md  (예시)
mkdir -p wiki/1_기초한의학/{원전,진단학,한방생리학,한방병리학,경락경혈학,침구학,본초학,방제학,기공학,의사학} \
         wiki/4_임상한의학/내과/{비계,폐계,심계,간계,신계} \
         wiki/4_임상한의학/{외과,산부인과,소아과,신경정신과,안이비인후피부과,침구과,추나의학,재활의학,사상의학} \
         wiki/6_보건기타/{예방한의학,한의학정책및제도,보건학}
```

문서는 AGENTS.md의 위키 문서 구조(개요·근거요약·근거표·임상적용·경혈/처방연관·참고문헌·미충족 근거)를 따른다.

**DOI/PMID 링크 처리 규칙**: 문서에 등장하는 모든 DOI와 PMID는 반드시 링크로 처리한다. DOI는 `https://doi.org/<DOI>`(예: `[10.1000/example](https://doi.org/10.1000/example)`), PMID는 `https://pubmed.ncbi.nlm.nih.gov/<PMID>/`(예: `[PMID 123456](https://pubmed.ncbi.nlm.nih.gov/123456/)`)로 연결한다. 근거 표·참고문헌·본문의 모든 DOI/PMID에 적용한다.

**미충족 근거/추가 연구 필요 섹션 서술 톤**: 이 섹션은 연구가 부실하거나 질이 나쁘다는 식으로 서술하지 않는다. 대신 "현재 근거 수준으로는 임상적 결론을 내리기에는 아직 연구가 충분하지 않다", "추후 더 많은/더 정밀한 연구(대규모 임상시험·체계적 고찰 등)가 필요하다"와 같이 **한의학에 우호적이고 건설적인 방향으로** 서술한다. 이미 확인된 한의학적 가치·긍정적 결과를 인정하고, 이를 뒷받침할 추가 연구를 제안하는 어조를 유지한다.
YAML front matter를 넣지 않고, 제목 헤딩 아래에 출처·근거수준·작성일을 본문에 서술한다.

임상한의학 각 과(내과·외과·산부인과·소아과·신경정신과·안이비인후피부과·침구과·추나의학·재활의학·사상의학) 밑에는 **교과서적 편제로서 해당 임상 각과에서 다루는 각 질환에 대해 서술**한다.

**각 문서에는 반드시 Q&A 파트를 포함한다.** AGENTS.md의 위키 문서 구조에 Q&A 섹션을 추가해, 해당 주제에 대해 한의사 임상가가 실제 진료 현장에서 자주 묻는 질문과 그에 대한 근거 기반 답변을 3~5개 정도의 문답(Q) 형식으로 수록한다. 각 답변에는 해당 근거가 있으면 DOI/PMID와 근거수준을 명시하고, 근거가 없으면 "근거 미확인"으로 정직하게 밝힌다. 문답은 임상 적용 관점에서 한의학적으로 우호적인 해석을 담는다.

질환 문서는 **KCD-8 코드를 본문 개요 첫 문단에 명시**한다 (예: `요통 (KCD-8: M54.5)`). 파일명에는 코드를 넣지 않는다.

**질환 문서의 세분화된 목차 구성**: 질환 문서는 단순 개괄이 아닌, 임상 한의사가 전반적으로 활용할 수 있도록 목차를 세분화해 작성한다. 질환에 따라 다음 항목을 적절히 포괄한다:
- **개요** (정의·역학·KCD-8 코드)
- **병인 병기(病因病機)** — 한의학적 원인·병리 기전
- **병태생리** — 서양의학적 병태생리 및 진단 접근
- **변증(辨證) 및 감별 진단** — 한의학적 변증 분류, KCD/감별
- **치료** — 침구·처방·본초·추나 등 한의학적 치료법 및 임상 근거
- **예후** — 치료 반응 및 경과 예측
- **관리** — 생활 관리, 예방, 재발 방지, 환자 교육
- **Q&A** — 임상 현장 자주 묻는 질문
- **근거 요약·근거 표·참고문헌·미충족 근거** — AGENTS.md 구조 유지

각 섹션은 근거 기반으로 작성하며, 해당 질환의 특성에 맞춰 목차를 유연하게 세분화·조정한다.

본초·경혈·처방(방제) 이름은 **한의과대학 교과서적 표준 표기**를 쓴다 (예: 경혈 `합곡(LI4)`, 처방 `보중익기탕`, 본초 `인삼`·`백출`). 지방·약어·비표준 표기를 피하고 최초 등장 시 한자·약리학적 표기를 병기한다.

**한의학 용어 한자 병기 규칙**: 한자로 표기할 수 있는 한의학 용어(본초·경혈·처방·원전·진단·병리·증후·기혈·경락 등)는 본문에서 해당 한글 용어 뒤에 괄호로 한자를 병기한다 (예: `보중익기탕(補中益氣湯)`, `합곡(合谷)`, `음허(陰虛)`, `기혈(氣血)`). 최초 등장 시 반드시 병기하고, 이후 동일 용어는 반복 병기하지 않는다.

위키 제목(파일명/문서 제목)은 **한글·영어·한자·외래어(원어)를 병기**할 수 있다 (예: `합곡(合谷, LI4)`, `보중익기탕(補中益氣湯)`). **동음이의/다의어가 필요한 문서는 동음이의 분류로 따로 처리**한다 — 파일명에 구분자를 명시하거나(예: `인삼(人蔘)` vs `인삼(삼, Panax ginseng)`), 괄호 표기로 구분하고 폴더 `README.md` 목차에 항목을 명시한다.

## 2. 위키 보강 (기존 문서 개선)

기존 문서에 근거를 추가·갱신하거나 새 논문을 반영한다.

### 2-1. 문서 재검증 및 근거 갱신

```bash
# 기존 문서의 주제로 최신 논문 재검색
curl -s "$BASE/search?q=문서주제어&analyzed=1&per_page=100"
```

- 최신 `analyzed_at`/`fetched_at` 기준으로 근거 표를 갱신한다.
- 누락된 근거 수준(연구 유형) 라벨을 보완한다.
- AI 요약(`answer`/`clinical_summary`)을 재대조해 정확성을 확인한다.

### 2-2. 부족한 근거 파악

```bash
# 연구 유형별 분포 확인 (통계)
curl -s "$BASE/stat"
```

- 특정 문서의 근거 수준이 낮거나 비어 있으면 `cat` 필터로 더 높은 근거(체계적 고찰/메타분석)를 추가로 수집한다.

## 3. 검증 및 품질 점검

### 3-0. RUN.md 워크플로우 자동 실행 (작업 후 반드시)

작업이 끝날 때마다 아래 커맨드로 검증·링크 점검·빌드를 한 번에 실행한다.

```bash
# opencode 커맨드 (검증 + 링크 점검 + 빌드)
/run

# 또는 스크립트 직접 실행
python3 scripts/run.py
python3 scripts/run.py --skip-build   # 빌드 생략
python3 scripts/run.py --health       # API 헬스 체크 포함
```

### 3-1. 자동 검증 스크립트

```bash
# 링크·KCD·표준 표기 검증
python3 scripts/validate.py
```

### 3-2. 링크/문서 구조 점검

```bash
# 교차 참조(상대 링크) 끊어진 곳 확인
rg -o '\]\(\.\./[^)]+\)' wiki/ | sort -u
```

### 3-3. 근거 무결성 점검

- 모든 임상 주장에 DOI/PMID가 있는지 확인
- 비한의학 논문이 섞였는지 `is_korean_medicine` 필드로 확인
- AI 요약을 인용으로 사용하지 않았는지 대조

## 4. 헬스 체크

```bash
# API 정상 여부
curl -s "$BASE/health"
```

## 5. 정적 사이트 빌드

```bash
# 빌드 (site/ 출력)
python3 -m mkdocs build

# 로컬 미리보기
python3 -m mkdocs serve
```

## 6. 위키 자동 증식·보강 (opencode 반복 실행)

`scripts/loop.py`가 opencode를 호출해 RUN.md 워크플로우를 반복 실행한다.
목차에서 미작성 문서를 골라 근거 기반으로 작성하고, 기존 문서의 오류·근거를 갱신한다.

```bash
# 무한 반복 (Ctrl+C로 중단)
python3 scripts/loop.py

# 20회만 실행
python3 scripts/loop.py --max 20

# 실행 사이 5초 대기
python3 scripts/loop.py --interval 5

# 체크포인트 초기화 (처리 이력 삭제)
python3 scripts/loop.py --reset
```

- 진행 이력은 `scripts/.progress.json`, 로그는 `scripts/loop.log`에 기록된다.
- 연속 3회 실패 시 자동 중단한다.

## 7. 추천(좋아요) 백엔드

정적 사이트의 추천 버튼이 집계 API를 호출한다. 추천 수는 SQLite(`server/likes.db`)에 저장된다.

```bash
# 백엔드 실행 (0.0.0.0:8000)
python3 server/app.py

# 포트 지정
python3 server/app.py --port 9000
```

- 추천 버튼: 각 문서 상단에 자동 주입 (`wiki/assets/like.js`)
- 추천 순위 페이지: `wiki/추천순위.md` (nav에 등록됨)
- API 엔드포인트: `POST /api/like`, `GET /api/likes`, `GET /api/rank`, `GET /health`
- 프론트엔드가 API 주소를 바꾸려면 `LIKE_API` 전역 변수로 지정한다 (기본 `http://localhost:8000`).

## 유의사항

- API 과호출 방지를 위해 `per_page`는 최대 100, 필요한 페이지만 요청한다.
- 문서 작성/보강 후 반드시 검증 명령어로 링크·근거 무결성을 점검한다.

## 커밋 규칙

- **작업 완료 시 자동으로 커밋한다.** 사용자가 별도로 요청하지 않아도 작업이 끝나면 변경 사항을 커밋한다.
- 커밋 전 `git status`/`git diff`로 의도한 파일만 스테이징하고, 시크릿·불필요 파일은 제외한다.
- 커밋 메시지는 변경 내용을 간결히 요약한다 (예: `docs: 위키 분류 체계 초기화`, `feat: 논문 검색 스크립트 추가`).
