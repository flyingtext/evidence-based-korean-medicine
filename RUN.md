# RUN.md — 위키 작성·보강 명령어

이 문서는 근거 기반 한의학 위키를 작성·보강·검증하는 데 쓰는 명령어 모음이다.
모든 데이터는 `https://med.symbolicinfo.com` API에서 가져온다. 작업 시 AGENTS.md의 핵심 원칙(근거 우선, 출처 명시)을 반드시 따른다.

**독자 가정과 서술 톤**: 독자를 **한의사 임상가**로 가정한다. 따라서 각 근거 기반 서술은 한의학 치료법(침구·처방·본초·추나 등)에 **우호적(호의적) 관점**으로 작성한다. 즉, 근거가 유의하거나 긍정적인 결과를 보인 연구를 강조해 제시하고, 한의학적 접근의 임상적 가치와 적용 가능성을 적극적으로 부각한다. 다만 근거가 부족하거나 부정적인 결과도 숨기지 않고 정직하게 기술하되, 그 해석은 한의학적으로 유의미한 방향(치료 원리·개선 방향·추가 연구 제안)으로 유도한다. 출처 명시는 AGENTS.md 원칙을 그대로 유지한다.

## 공통

```bash
BASE="https://med.symbolicinfo.com"
```

**실행 프롬프트 노출 금지 (강제)**: 위키 문서는 최종 산출물이다. 문서 본문에는 이 워크플로우의 실행 지시(프롬프트)가 어떤 형태로도 노출되어서는 안 된다. 구체적으로 다음을 금지한다:
- "RUN.md 원칙에 따라", "AGENTS.md 규칙에 따라", "프롬프트에 따라" 등 워크플로우·명령어 문서를 언급하는 문구
- 검색·증식·보강·검증·빌드 등 작업 절차나 명령어의 언급
- 근거 정리 방향·서술 톤 등 실행 지시가 그대로 베껴진 문구 (예: "한의학적으로 우호적인 관점에서 근거를 정리한다")

문서는 항상 독자를 위해 근거·임상 정보만 서술한다. 검증 시에도 문서 본문에 프롬프트 혼입이 있는지 확인한다.

## 1. 위키 생성 (신규 문서 작성)

새 문서는 주제 → 논문 수집 → 문서 작성 순서로 진행한다.

### 1-0. 새 표제어 주제 선정 (기존 문서 언급 빈도 우선)

새로 작성할 표제어(본초·경혈·처방·질환·용어 등)를 고를 때는 **기존 위키 문서 본문에서 가장 많이 언급되지만 아직 문서가 없는 개념을 우선 선정**한다. 기존 문서에서 반복적으로 등장하는 개념은 위키 내 교차 참조·내부 링크 수요가 크고 임상적 중요도가 높은 경우가 많기 때문이다. 선정 시 `rg` 등으로 기존 문서의 언급 빈도를 조사해 상위 개념을 후보로 삼고, 그중 아직 문서가 없는 것을 우선 작성한다.

```bash
# 기존 문서에서 자주 언급되는 경혈·처방·본초 후보 조사 (예시)
rg -o '내관|삼음교|태충(LR3)|백회|곡지|풍지|신수' wiki/ -g '*.md' | sort | uniq -c | sort -rn
rg -o '육군자탕|반하사심탕|오령산|소청룡탕|가미소요산|갈근탕' wiki/ -g '*.md' | sort | uniq -c | sort -rn
rg -o '대조|계지|복령|건강|시호|천마|황련|지황' wiki/ -g '*.md' | sort | uniq -c | sort -rn
```

이렇게 조사한 상위 언급 개념 중 아직 문서가 없는 것을 우선 선정한 뒤, 아래 1-1부터 논문 수집·문서 작성을 진행한다.

### 1-1. 관련 논문 수집 (분석 완료, 한의학, 인체 연구)

```bash
# 주제별 논문 검색 (analyzed=1 우선 사용)
curl -s "$BASE/search?q=검색어&km=1&human=1&analyzed=1&per_page=20"
```

주요 필터 조합 예시:

```bash
# 요통 관련 임상시험/메타분석
curl -s "$BASE/search?q=요통&cat=clinical_trial,meta_analysis&analyzed=1&per_page=50"
curl -s "$BASE/search?q=low back pain&cat=systematic_review&analyzed=1&per_page=50"

# 특정 처방/경혈 관련
curl -s "$BASE/search?q=보중익기탕&analyzed=1&per_page=50"
curl -s "$BASE/search?q=합곡&analyzed=1&per_page=50"
```

### 1-2. 문서 파일 작성

수집한 논문을 바탕으로 다음 명령으로 문서를 생성한다. **문서 생성 후에는 반드시 `mkdocs.yml`의 `nav` 섹션에 해당 문서를 추가하여 내비게이션에 반영해야 한다.**

```bash
# wiki/질환/요통.md  (예시)
mkdir -p wiki/1_기초한의학/{원전,진단학,한방생리학,한방병리학,경락경혈학,침구학,본초학,방제학,기공학,의사학} \
         wiki/4_임상한의학/내과/{비계,폐계,심계,간계,신계} \
         wiki/4_임상한의학/{외과,산부인과,소아과,신경정신과,안이비인후피부과,침구과,추나의학,재활의학,사상의학} \
         wiki/6_보건기타/{예방한의학,한의학정책및제도,보건학}
```

문서는 **AGENTS.md의 위키 문서 구조에 따라 표제어에 맞게 독자적으로 구성**한다. 문서 구조는 고정 템플릿이 아니므로, **다른 위키 파일이나 `_template.md`를 참조해 같은 목차·서술 틀로 찍어내지 않는다.** 각 표제어(본초·경혈·처방·질환·용어)의 성격과 확보한 논문 근거에 맞춰 소제목·서술 순서를 새로 설계한다. 필수 공통 요소(세분화된 목차, 문장별 근거 각주, Q&A 섹션)만 지키고 나머지는 주제에 맞게 자유롭게 구성한다. 근거 표를 별도로 두지 않고, 모든 출처 정보는 본문의 문장별 각주와 각주 정의로 통일한다.

**분류 단계 유연성**: 위키 분류는 대분류→중분류→소분류의 3단계에 얽매일 필요가 없다. **필요에 따라 더 하위로 분류를 뻗어나갈 수 있다.** 즉 소분류 아래에도 4단계·5단계 이상의 하위 폴더를 두어 상세하게 분류할 수 있고, 반대로 어떤 주제는 3단계보다 적게(2단계) 구성해도 된다. 예를 들어 `임상한의학/내과/비계/소화불량/기능성소화불량`처럼 중분류·소분류를 추가로 세분화하거나, 반대로 소분류가 필요 없는 주제는 그대로 두어도 된다. 목표는 **모든 문서가 논리적으로 명확히 정리·검색 가능하도록** 하는 것이며, 단계 수에 얽매이지 않고 내용의 깊이와 관련성에 맞춰 폴더 구조를 자유롭게 구성한다. 폴더를 새로 만들면 그 하위 구조에 맞춰 각 상위 폴더의 `README.md` 목차와 `mkdocs.yml`의 `nav`를 갱신한다.

**DOI/PMID 링크 처리 규칙**: 문서에 등장하는 모든 DOI와 PMID는 반드시 링크로 처리한다. DOI는 `https://doi.org/<DOI>`(예: `[10.1000/example](https://doi.org/10.1000/example)`), PMID는 `https://pubmed.ncbi.nlm.nih.gov/<PMID>/`(예: `[PMID 123456](https://pubmed.ncbi.nlm.nih.gov/123456/)`)로 연결한다. 각주 정의·본문의 모든 DOI/PMID에 적용한다.

**문장별 각주(출처) 처리 규칙**: 본문의 근거 기반 서술은 **문장 단위로 각주(footnote)를 달아 출처를 명시**한다. 각 근거 문장(임상적 주장·효능·효과·수치 등)의 끝에 `[^1]`, `[^2]`와 같은 각주 마커를 붙이고, 문서 하단에 해당 각주 정의를 작성한다. 각주 정의에는 해당 근거 논문의 **제목·연구유형·저널·DOI/PMID(링크)**을 기재한다. 문장이 여러 근거를 인용하면 각주 안에 여러 출처를 나열한다. 근거가 없는 문장은 각주에 "근거 미확인"이라고 명시한다. (예: `요통에 침 치료가 진통 효과를 보였다.[^1]` / `[^1]: Kim J et al. 임상시험, DOI: [10.1000/xxx](https://doi.org/10.1000/xxx)`)

**각주 정의로 출처 정보 통일(강제)**: 문서의 출처 정보는 **각주 정의로만** 표기한다. 별도의 근거 표나 `## 참고문헌` 섹션(번호 목록)을 두지 않는다. 각주 정의가 참고문헌의 역할을 대신하므로, 본문에서 인용한 모든 논문은 각주로 정의하고 참고문헌 목록은 중복 작성하지 않는다. 각주 정의는 문서 맨 끝에 **별도의 헤더 없이, 불릿 기호(`-`) 없이** 각주 마커(`[^n]:`)로만 나열한다.

**핵심 키워드 내부 링크 규칙**: 문서 본문에서 등장하는 **핵심 키워드(본초·경혈·처방·질환·용어)가 위키 내 다른 문서로 존재하면 상대 링크로 내부 연결**한다. 최초 등장 시 한 번 링크하고 반복 등장은 링크하지 않는다. 링크는 AGENTS.md 교과서적 표준 표기를 기준으로 한다 (예: `[합곡(合谷, LI4)](1_기초한의학/경락경혈학/합곡(合谷, LI4).md)`, `[보중익기탕(補中益氣湯)](1_기초한의학/방제학/보중익기탕(補中益氣湯).md)`, `[요통(腰痛)](4_임상한의학/재활의학/요통.md)`). 존재하지 않는 문서로는 링크하지 않는다(링크 대상 파일이 있는지 반드시 확인).

YAML front matter를 넣지 않고, 제목 헤딩 아래에 출처·작성일을 본문에 서술한다.

임상한의학 각 과(내과·외과·산부인과·소아과·신경정신과·안이비인후피부과·침구과·추나의학·재활의학·사상의학) 밑에는 **교과서적 편제로서 해당 임상 각과에서 다루는 각 질환에 대해 서술**한다.

**각 문서에는 반드시 Q&A 파트를 포함한다.** AGENTS.md의 위키 문서 구조에 Q&A 섹션을 추가해, 해당 주제에 대해 한의사 임상가가 실제 진료 현장에서 자주 묻는 질문과 그에 대한 근거 기반 답변을 3~5개 정도의 문답(Q) 형식으로 수록한다. 각 답변에는 해당 근거가 있으면 DOI/PMID를 명시하고, 근거가 없으면 "근거 미확인"으로 정직하게 밝힌다. 문답은 임상 적용 관점에서 한의학적으로 우호적인 해석을 담는다.

질환 문서는 **KCD-8 코드를 본문 개요 첫 문단에 명시**한다 (예: `요통 (KCD-8: M54.5)`). 파일명에는 코드를 넣지 않는다.

**포괄적 검색어 확장(모든 문서 공통)**: 문서를 작성·보강할 때는 단일 검색어에 그치지 않고 **포괄적인 검색어 확장(검색어 확장, query expansion)**을 수행해 다양하고 풍성한 근거를 수집한다. 주제의 한글명·영문명·동의어·관련 증상·관련 처방·관련 경혈·관련 한의학 변증 용어 등을 조합해 여러 검색어로 반복 검색하고, `cat`(연구 유형)·`km`·`human`·`lit`·`source` 필터를 다양하게 조합해 임상시험·체계적 고찰·메타분석·관찰연구·증례 등 다양한 연구 유형의 논문을 확보한다. 이렇게 확장된 검색 결과를 종합해 본문을 작성함으로써, 한쪽 검색어에 치우치지 않고 해당 주제에 대한 근거를 폭넓고 풍성하게 반영한다. 이 원칙은 질환 문서뿐 아니라 본초·경혈·처방·기타 모든 문서에 공통 적용한다.

**연구 유형 우선 + 임상 참고 논문 필수 인용(모든 문서 공통)**: 논문 선별 시 최대한 높은 연구 유형(체계적 고찰/메타분석 > 임상시험 > 관찰연구 > 증례)의 논문을 우선 선별한다. 다만 낮은 연구 유형이라도 임상에 참고가 되는 논문(관찰연구·증례·파일럿 등)은 반드시 인용한다. 검색으로 확보한 관련 논문을 임의로 버리지 않고, 높은 연구 유형을 우선하되 임상적 가치가 있는 논문은 빠짐없이 본문(각주)에 반영한다. 즉, 검색 결과가 수백 편이어도 주제와 관련된 논문은 임의로 버리지 말고 하위 표제어·변증·치료법별로 나누어 최대한 많이 인용한다.

**최소 인용 논문 수(모든 문서 공통)**: 각 문서의 각주 정의에는 **서로 다른 DOI/PMID 논문을 최소 15편(가능하면 20편 이상) 수록**한다. 논문 수가 적어 근거가 빈약한 문서가 되지 않도록, `per_page=100`으로 검색하고 필요 시 여러 페이지를 순회해 후보를 전수 수집한다. 같은 DOI의 논문은 하나로 병합(중복 제거)하고, 임상시험·체계적 고찰·메타분석·관찰연구·증례 등 다양한 연구 유형을 고르게 포함한다. `scripts/search.py`는 `--target`과 다중 페이지 순회로 목표 수량을 확보하는 것을 돕는다:

```bash
# 요통: 목표 25편 이상 확보 (DOI 중복 제거, 다중 페이지 순회)
python3 scripts/search.py "요통" --km --human --analyzed --per-page 100 --target 25
```

만약 검색으로도 총 15편 미만의 논문만 확보되는 주제라면, 그 사실과 확보한 논문 수를 문서에 명시하고 부족한 이유를 정직하게 기술한다.

**선별하더라도 최대한 많이 인용하는 방침(모든 문서 공통)**: 문서 작성·보강 시 검색으로 확보한 논문을 선별하더라도, **선별된 논문은 최대한 많이 인용**한다. 즉, 주제와 관련된 논문은 임의로 버리지 않고 본문(각주)에 빠짐없이 반영한다. 검색 결과가 많아 문서가 길어지더라도 하위 표제어·변증·치료법별로 나누어 모두 수록하며, 최소 수량(15편)만 채우는 데 그치지 않는다. 선별 기준은 주제 관련성·연구 유형 다양성이다.

**검색된 모든 논문 전수 분석·인용 규칙(모든 문서 공통)**: 검색 API로 수집한 논문은 **최대한 빠짐없이 전수 분석하여 인용**한다. `per_page=100`으로 검색하고 필요 시 여러 페이지를 순회해 후보를 전수 수집한 뒤, **같은 DOI/PMID의 논문은 하나로 병합(중복 제거)하고, 그 결과물인 모든 논문을 본문(각주)에 빠짐없이 반영**한다. 단순히 최소 수량(15편)만 채우는 데 그치지 않고, 검색으로 확보된 논문 중 주제와 관련된 것은 하나도 누락하지 않도록 한다. 각 논문의 연구 유형·환자 수·DOI/PMID·AI 임상 요약(`answer`/`clinical_summary`)을 각주 정의에 기재하고, 본문의 해당 서술에 각주로 연결한다. 검색 결과가 많아 문서가 지나치게 길어질 경우에도, 논문을 임의로 버리지 말고 하위 표제어·변증·치료법별로 나누어 모두 수록한다.

**하위 표제어(세분화 분류) 목록화 규칙**: 하나의 표제어가 여러 하위 유형·아형·세부 항목을 포괄하는 경우, 단일 문서로 퉁치지 않고 **하위 표제어를 명시적으로 목록화**한다. 예를 들어 고혈압 문서는 본태성 고혈압·폐성 고혈압·신성 고혈압 등 다양한 고혈압 종류를 하나로 묶지 않고, 각 하위 유형을 별도로 구분해 다룬다. 이 규칙은 질환뿐 아니라 **본초·경혈·처방·증후·기타 모든 표제어**에 공통 적용한다.

- **하위 표제어 식별**: 해당 표제어의 하위 유형·아형·변증·부위별·병기별 구분을 조사해 목록화한다 (예: 고혈압 → 본태성/폐성/신성, 두통 → 편두통/긴장성/군발성, 인삼 → 인삼/홍삼/백삼 등).
- **문서 내 목차 구성**: 하위 표제어가 여러 개면 문서 본문에 **"하위 표제어" 또는 "분류" 섹션**을 두어 각 하위 항목을 나열하고, 각 항목에 대해 정의·특징·한의학적 변증·치료 근거를 간략히 서술한다.
- **별도 문서 분리**: 하위 표제어가 독립적으로 충분한 근거와 임상적 중요성을 가지면 **별도 문서로 분리**하고, 상위 문서에서 상대 링크로 연결한다 (예: `고혈압.md` → `본태성고혈압.md`, `폐성고혈압.md`). 분리한 하위 문서는 폴더 `README.md` 목차에 명시한다.
- **동음이의/다의어 구분**: 하위 표제어가 동음이의·다의어로 식별이 필요한 경우 AGENTS.md의 동음이의 분류 규칙(파일명 구분자·괄호 표기)을 적용한다.
- **KCD-8 코드**: 질환 하위 표제어는 각각의 KCD-8 코드를 개요 첫 문단에 명시한다 (예: 본태성 고혈압 `I10`, 폐성 고혈압 `I27.0`).

**질환 문서의 세분화된 목차 구성**: 질환 문서는 단순 개괄이 아닌, 임상 한의사가 전반적으로 활용할 수 있도록 목차를 세분화해 작성한다. 질환에 따라 다음 항목을 적절히 포괄한다:
- **개요** (정의·역학·KCD-8 코드)
- **병인 병기(病因病機)** — 한의학적 원인·병리 기전
- **병태생리** — 서양의학적 병태생리 및 진단 접근
- **진단(診斷) 및 감별 진단** — 질환의 진단 과정을 깊이 있게 서술. 병력 청취(주요 증상·경과·악화 요인), 이학적 검사(시진·촉진·신경학적·정형외과적 검사 등), 영상 검사(X-ray·CT·MRI·초음파 등), 실험실 검사(혈액·소변·객관적 지표), 기능 검사(알고미터·위전도·자율신경 검사 등)를 질환 특성에 맞춰 구체적으로 나열하고, 각 검사의 목적·시행 근거·진단적 의미를 기술한다. 또한 **양방 진단 기준(임상 진단 기준·분류 체계·단계/등급)**을 명시하고, 감별해야 할 유사 질환(red flag·위험 신호 포함)을 구체적으로 나열하여 서술한다. 근거가 있는 검사는 DOI/PMID를 명시하고, 없는 항목은 "교과서적 근거"로 표기한다. **교과서적 임상 진단 이론 수준의 내용은 굳이 근거 등급을 매기지 않는다.** 진단 이론·병기·병태생리 등 한의과대학 교과서에 표준적으로 수록된 이론적 내용은 근거 등급을 매기지 않고 "교과서적 근거"라고만 표기한다.
- **변증(辨證)** — 한의학적 변증 분류(각 변증의 주증·설진·맥상·치법)와 KCD 코드, 변증별 감별
- **치료** — 침구·처방·본초·추나 등 한의학적 치료법 및 임상 근거
- **예후** — 치료 반응 및 경과 예측
- **관리** — 생활 관리, 예방, 재발 방지, 환자 교육
- **Q&A** — 임상 현장 자주 묻는 질문
- **근거 요약·각주 정의** — AGENTS.md 구조 유지

각 섹션은 근거 기반으로 작성하며, 해당 질환의 특성에 맞춰 목차를 유연하게 세분화·조정한다.

본초·경혈·처방(방제) 이름은 **한의과대학 교과서적 표준 표기**를 쓴다 (예: 경혈 `합곡(LI4)`, 처방 `보중익기탕`, `소건중탕(小建中湯)`, `축천환(縮泉丸)`, 본초 `인삼`·`백출`). 지방·약어·비표준 표기를 피하고 최초 등장 시 한자·약리학적 표기를 병기한다. 표준 표기가 아닌 오기(예: 小建中湯을 "소견중탕"으로, 縮泉丸을 "수권환"으로 적는 것)는 사용하지 않는다.

**한의학 용어 한자 병기 규칙**: 한자로 표기할 수 있는 한의학 용어(본초·경혈·처방·원전·진단·병리·증후·기혈·경락 등)는 본문에서 해당 한글 용어 뒤에 괄호로 한자를 병기한다 (예: `보중익기탕(補中益氣湯)`, `합곡(合谷)`, `음허(陰虛)`, `기혈(氣血)`). 최초 등장 시 반드시 병기하고, 이후 동일 용어는 반복 병기하지 않는다.

**한자 발음 표기 규칙 (강제)**: 본문에서 한자를 병기할 때 해당 한자(한자어)의 읽기는 **반드시 표준 한국어 한자 발음(국어의 표준 한자음)**으로만 표기한다. 이는 선택 사항이 아니라 **무조건 지켜야 하는 강제 규칙**이다. 즉, 국어사전에 등재된 한국식 한자음(예: 合谷 → 합곡, 陰虛 → 음허, 補中益氣湯 → 보중익기탕)에 따라 한글로 읽으며, **중국식 발음(병음·광동어 등)·일본식 발음·지방 발음·영어식 표기는 어떤 경우에도 사용하지 않는다.** 한자 표기의 목적은 어원·본뜻·동음이의 구분을 위한 것이므로, 읽기는 항상 표준 한국어 한자 발음을 기준으로 한다. 본초·경혈 등 한의학 고유 명칭도 교과서적 한국 한자음 표기를 따른다. **한자어를 한글로 읽을 때는 반드시 한국식 한자음으로 읽고, 중국어 발음(예: 合谷을 "허구"로, 陰虛를 "인쉬"로)으로 읽는 것을 금지한다.** 예: 十味龍膽花 → **십미용담화**(十=십, 龍=용, 膽=담), 痰熱清 → **담열청**(痰=담), 咳喘平湯 → **해천평탕**(咳=해), 柴僕湯 → **시복탕**(僕=복), 金振口服液 → **금진구복액**(口服液=구복액), 加味玉屛風散 → **가미옥병풍산**(屛=병), 咳速寧顆粒 → **해속녕과립**(咳=해, 速=속, 寧=녕). 중국어 병음(예: Kesuning)을 그대로 한글로 옮긴 표기(예: 커수닝)는 금지한다.

위키 제목(파일명/문서 제목)은 **한글·영어·한자·외래어(원어)를 병기**할 수 있다 (예: `합곡(合谷, LI4)`, `보중익기탕(補中益氣湯)`). **동음이의/다의어가 필요한 문서는 동음이의 분류로 따로 처리**한다 — 파일명에 구분자를 명시하거나(예: `인삼(人蔘)` vs `인삼(삼, Panax ginseng)`), 괄호 표기로 구분하고 폴더 `README.md` 목차에 항목을 명시한다.

## 2. 위키 보강 (기존 문서 개선)

기존 문서에 근거를 추가·갱신하거나 새 논문을 반영한다.

**보강 방식 — 기존 내용을 보존하고 증분으로 추가(강제)**: 문서를 보강할 때는 기존 파트·서술을 삭제하거나 전체를 다시 쓰는 대신, **기존 부분을 그대로 두고 새로운 목차·소제목을 추가하거나 diff(증분) 방식으로 내용을 보강**한다. 즉, 기존 문서의 구성과 문장은 유지하고, 새로 확보한 논문 근거에 맞는 하위 표제어·변증·치료법·Q&A를 추가하거나 기존 각주에 새 논문을 덧붙이는 방식으로 진행한다. 문서 전체를 재작성하는 듯한 인상을 주는 대규모 교체는 피한다. 추가한 신규 목차·내용은 기존 톤·표기 규칙(한자 병기, 각주 정의)을 동일하게 따르고, `mkdocs.yml`의 `nav`에 반영이 필요한 구조 변경이면 함께 갱신한다.

**보강에는 오류 수정도 포함(강제)**: 보강은 새 근거를 추가하는 것만이 아니라, **기존 문서에 잘못된 내용(사실·수치·논문 인용 오류·연구 유형 라벨 오류 등)이나 잘못된 포맷(표기 규칙 위반, 각주 형식 오류, 깨진 링크, `mkdocs.yml` `nav` 불일치 등)이 발견되면 이를 정정·수리하는 것도 포함**한다. 오류를 발견하면 증분 보강 원칙과 별개로 즉시 수정하고, 수정 사실을 커밋 메시지에 반영한다. 신규 추가와 오류 수정을 함께 수행할 때는 각 변경을 구분해 정리한다.

### 2-1. 문서 재검증 및 근거 갱신

```bash
# 기존 문서의 주제로 최신 논문 재검색
curl -s "$BASE/search?q=문서주제어&analyzed=1&per_page=100"
```

- 최신 `analyzed_at`/`fetched_at` 기준으로 근거(각주 정의)를 갱신한다.
- 누락된 연구 유형 라벨을 보완한다.
- AI 요약(`answer`/`clinical_summary`)을 재대조해 정확성을 확인한다.

### 2-2. 부족한 근거 파악

```bash
# 연구 유형별 분포 확인 (통계)
curl -s "$BASE/stat"
```

- 특정 문서의 근거가 비어 있으면 `cat` 필터로 더 높은 연구 유형(체계적 고찰/메타분석)을 추가로 수집한다.

## 3. 검증 및 품질 점검

### 3-1. RUN.md 워크플로우 자동 실행 (작업 후 반드시)

작업이 끝날 때마다 아래 커맨드로 검증·링크 점검·빌드를 한 번에 실행한다.

```bash
# opencode 커맨드 (검증 + 링크 점검 + 빌드)
/run

# 또는 스크립트 직접 실행
python3 scripts/run.py
python3 scripts/run.py --skip-build   # 빌드 생략
python3 scripts/run.py --health       # API 헬스 체크 포함
```

### 3-2. 자동 검증 스크립트

```bash
# 링크·KCD·표준 표기 검증
python3 scripts/validate.py
```

### 3-3. 링크/문서 구조 점검

```bash
# 교차 참조(상대 링크) 끊어진 곳 확인
rg -o '\]\(\.\./[^)]+\)' wiki/ | sort -u
```

### 3-4. 근거 무결성 점검

- 모든 임상 주장에 DOI/PMID가 있는지 확인
- 비한의학 논문이 섞였는지 `is_korean_medicine` 필드로 확인
- AI 요약을 인용으로 사용하지 않았는지 대조

## 4. 헬스 체크

```bash
# API 정상 여부
curl -s "$BASE/health"
```

## 5. 정적 사이트 빌드

```bash
# 빌드 (site/ 출력)
python3 -m mkdocs build

# 로컬 미리보기
python3 -m mkdocs serve
```

## 6. 위키 자동 증식·보강 (opencode 반복 실행)

`scripts/loop.py`가 opencode를 호출해 RUN.md 워크플로우를 반복 실행한다.
새 표제어를 직접 선정해 근거 기반으로 작성하고, 기존 문서의 오류·근거를 갱신한다.

```bash
# 무한 반복 (Ctrl+C로 중단)
python3 scripts/loop.py

# 20회만 실행
python3 scripts/loop.py --max 20

# 실행 사이 5초 대기
python3 scripts/loop.py --interval 5

# 체크포인트 초기화 (처리 이력 삭제)
python3 scripts/loop.py --reset
```

- 진행 이력은 `scripts/.progress.json`, 로그는 `scripts/loop.log`에 기록된다.
- 연속 3회 실패 시 자동 중단한다.

## 유의사항

- API 과호출 방지를 위해 `per_page`는 최대 100, 필요한 페이지만 요청한다.
- 문서 작성/보강 후 반드시 검증 명령어로 링크·근거 무결성을 점검한다.
- 문서 본문에 실행 프롬프트(워크플로우 지시·명령어·시스템 규칙 문구)가 혼입되지 않았는지 검증한다. 혼입 시 즉시 제거한다.

## 커밋 규칙

- **작업 완료 시 자동으로 커밋한다.** 사용자가 별도로 요청하지 않아도 작업이 끝나면 변경 사항을 커밋한다.
- 커밋 전 `git status`/`git diff`로 의도한 파일만 스테이징하고, 시크릿·불필요 파일은 제외한다.
- 커밋 메시지는 변경 내용을 간결히 요약한다 (예: `docs: 위키 분류 체계 초기화`, `feat: 논문 검색 스크립트 추가`).
