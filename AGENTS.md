# AGENTS.md

이 프로젝트는 [https://med.symbolicinfo.com](https://med.symbolicinfo.com)의 API를 활용해 **근거기반 한의학(Evidence-Based Korean Medicine) 위키**를 만드는 프로젝트입니다.

## 프로젝트 목표

- 논문 검색 엔진 API를 기반으로 한의학 주제·처방·경혈·질환에 대한 위키 문서를 생성·관리
- 모든 문서는 **출처(논문)를 명시**하여 신뢰할 수 있는 정보를 제공
- 위키 문서를 논문 데이터와 자동 연동

## 기준 문서 (Gold Standard)

**모든 신규·보강 문서는 다음 세 기준 문서의 구조·깊이·인용 밀도를 달성하는 것을 목표로 작성한다.**

- `wiki/기초한의학/한방병리학/간혈허 (肝血虛, Liver Blood Deficiency).md` — 1,614줄, ~130건 인용, 5편 23절, KCD 질환군 9개
- `wiki/기초한의학/한방병리학/간기울결 (肝氣鬱結, Liver Qi Stagnation).md` — 1,170줄, 122건 인용, 5편 20절 + Q&A
- `wiki/기초한의학/한방병리학/신양허(腎陽虛, Kidney Yang Deficiency).md` — 1,451줄, 259건 인용, 5편 22절, KCD 질환군 11개

기준 문서가 갖춘 필수 요소(다편 구조, KCD 질환군별 6단 각론, 근거 한계 명시, 변증 층화 강조, 안전성 표, 추적 지표표, 환자 설명 요약, Q&A, 조섭표, 각주 부연 해석, 고전 인용 출처)는 RUN.md §0-1에 상세히 정리되어 있다. 작성 전 반드시 기준 문서 중 하나를 숙독한다.

> 신양허(腎陽虛) 문서는 259건 인용·11개 KCD 질환군을 다루며, 단일 증후 표제어가 남성생식·부인과·골격계·신장·호흡기·순환기·소화기·혈액·신경인지·피로면역·대사 등 광범위한 질환군에 파급되는 "선천지본(先天之本)" 변증의 전형을 보여준다. 이 문서는 특히 **① 변증-예후·변증-분자지표 상관 연구를 질환군마다 밀도 있게 인용**하고, **② 변증이 생물학적 실체로서 재현성·타당도를 갖는다는 방법론적 근거를 별도 절(7-12)로 정리**한 것이 특징이다.

## 핵심 원칙 (반드시 준수)

1. **근거 우선**: 모든 임상적 주장은 반드시 검색 API로 확인 가능한 논문 근거에 기반해야 함. 근거가 없는 주장은 명시적으로 "근거 미확인"이라고 적지 말고 자연스럽게 생략한다.
2. **출처 명시**: 문서의 모든 핵심 주장에는 DOI/PMID와 논문 제목을 함께 기록. 각주 정의에는 메타데이터와 함께 **근거의 의미를 한 줄로 부연 해석**을 덧붕인다 (기준 문서 참조). 각주 정의 형식·연구종류 한국어 라벨은 RUN.md §1-4-2-1을 따른다.
3. **연구 유형 우선 + 임상 참고 논문 필수 인용**: 논문 선별 시 최대한 높은 연구 유형(체계적 고찰/메타분석 > 임상시험 > 관찰연구 > 증례)의 논문을 우선 선별한다. 다만 낮은 연구 유형이라도 임상에 참고가 되는 논문(관찰연구·증례·파일럿 등)은 반드시 인용한다. 검색으로 확보한 관련 논문을 임의로 버리지 않고, 높은 연구 유형을 우선하되 임상적 가치가 있는 논문은 빠짐없이 본문(각주)에 반영한다.
4. **검색된 논문 전수 인용**: `per_page=100`으로 전수 수집 후 같은 DOI/PMID는 병합하고, 주제와 관련된 논문은 하나도 누락하지 않고 본문(각주)에 반영한다. 최소 인용 수(증상·질환 100편, 처방·본초·경혈 50편)를 채우는 데 그치지 않고 전수 인용한다.
5. **AI 생성물 명시**: AI 요약(`answer`, `clinical_summary`) 사용 시 이를 밝히고 원문과 대조 가능하도록 함.
6. **과장 금지**: 상업적 효능 주장, 절대적 효능 표현 금지.
7. **근거 한계 명시**: 치료표·변증표·감별표 뒤에 "이 표는 임상 틀이지 동일 근거수준의 권고가 아니다" 식의 한계 문구를 부착한다. 기준 문서의 서술 방식을 따른다.
8. **변증 층화 강조**: "변증 없는 관행적 취혈/처방은 근거에 부합하지 않는다"는 원칙을 각 질환군 치료 섹션에 반복 명시한다.
9. **실행 프롬프트 노출 금지**: 위키 문서는 최종 산출물이며, 문서 본문에 작성·보강 워크플로우의 실행 지시(프롬프트)가 노출되어서는 안 된다. "RUN.md 원칙에 따라", "AGENTS.md 규칙에 따라" 등과 같은 작업 지시·워크플로우 언급, 검색·증식·검증 명령어, 시스템 규칙 문구를 문서 내용에 포함하지 않는다. 문서는 독자를 위해 근거·임상 정보만 서술한다.

## API 레퍼런스

모든 엔드포인트는 `GET` 방식이며 JSON 반환, CORS 활성화.

베이스 URL: `https://med.symbolicinfo.com`

### 1. 논문 검색 — `GET /search`

`articles` / `article_analysis` 테이블을 DOI 기준으로 그룹화하여 검색. 같은 논문이 PubMed·Crossref 모두에 있으면 하나로 병합.

```
GET /search?q=검색어&page=1&per_page=20&analyzed=1
```

| 파라미터 | 설명 | 기본값 |
|---|---|---|
| `q` | 검색어 (제목·저자·저널·DOI·PMID·PICO·임상요약 부분 일치) | 없음 |
| `page` | 페이지 번호 | 1 |
| `per_page` | 페이지당 개수 (1~100) | 20 |
| `analyzed` | 분석 완료만 (`1`/`true`) | false |
| `km` | 한의학 여부 (1/0) | 없음 |
| `human` | 인체 연구 여부 (1/0) | 없음 |
| `lit` | 문헌 고찰 여부 (1/0) | 없음 |
| `cat` | 연구 유형 (`clinical_trial`, `systematic_review`, `meta_analysis`, `observational_study`, `case_report`, `experimental_study`, `review`, `guideline`, `other`) | 없음 |
| `source` | 소스 필터: `pubmed`, `crossref`, `both`, `all` | all |
| `ref` | 대표 행 선택: `pubmed`, `crossref`, `both`, `all` | all |
| `pub_from` / `pub_to` | 출판일 범위 (YYYY-MM-DD) | 없음 |
| `kw` | 키워드 필터 (콤마 구분, AND 조건) | 없음 |

**응답 구조**:

```
{
  "q", "page", "per_page", "total", "total_pages",
  "analyzed", "km", "human", "lit", "cat", "source", "ref",
  "items": [ { 논문 객체 }, ... ]
}
```

**논문 객체 주요 필드**:

| 필드 | 설명 |
|---|---|
| `title` | 논문 제목 |
| `authors` | 저자 목록 |
| `journal` | 저널명 |
| `doi` | DOI |
| `pmid` | PubMed ID |
| `pub_date` | 출판일 |
| `url` | 원문 링크 |
| `source` / `in_pubmed` / `in_crossref` | 소스 정보 |
| `research_category` | 연구 유형 (임상시험/체계적 고찰 등) |
| `is_korean_medicine` | 한의학 여부 |
| `is_human_study` | 인체 연구 여부 |
| `is_literature_review` | 문헌 고찰 여부 |
| `patient_count` | 환자 수 |
| `question` | AI가 생성한 핵심 연구 질문 |
| `answer` | AI 요약 답변 |
| `clinical_summary` | 임상 요약 (실용적 함의) |
| `pico_p` / `pico_i` / `pico_c` / `pico_o` | PICO 구성요소 |
| `method_specific_acupoint` | 사용된 특정 경혈 |
| `method_specific_herbal` / `method_specific_herbal_formula` | 사용된 약재/처방 |
| `keywords` | 키워드 배열 |
| `analyzed_at` / `fetched_at` | 분석·수집 시각 |

### 2. 전체 통계 — `GET /stat`

전체 논문 수, 분석 현황, 연구 유형 분포, 상위 저널, 소스별 분포, 환자 수 합계 등.

### 3. 헬스 체크 — `GET /health`

### 예시

```bash
curl "https://med.symbolicinfo.com/search?q=cancer&analyzed=1"
curl "https://med.symbolicinfo.com/search?q=acupuncture&km=1&human=1&per_page=50"
curl "https://med.symbolicinfo.com/stat"
```

## 위키 파일 구조

위키는 **순수 마크다운(`.md`) 파일**로 구성한다. 문서는 주제별 폴더로 분류한다:

```
wiki/
├── README.md            # 위키 홈/인덱스
├── 기초한의학/        # 원전 · 진단학 · 한방생리학 · 한방병리학 · 경락경혈학 · 본초학 · 방제학 · 기공학 · 의사학
├── 임상한의학/        # 내과(장부) · 산부인과 · 소아과 · 신경정신과 · 안이비인후피부과 · 침구과 · 추나의학 · 재활의학 · 사상의학
└── 보건기타/          # 예방한의학
```

**분류 체계**: 한의과대학 예과·본과 교과과정을 참고해 **대분류(학문 분야) → 중분류(과목) → 소분류(개별 문서)** 3단계로 구성한다.

- **대분류** = 최상위 폴더 (기초한의학, 임상한의학, 보건기타)
- **중분류** = 대분류 아래 과목 폴더 (예: 기초한의학의 `본초학`/`방제학`/`경락경혈학`, 임상한의학 내과의 `비계`/`폐계`/`심계`/`간계`/`신계`)
- **소분류** = 개별 문서 파일 (예: `보중익기탕.md`, `합곡.md`, `요통.md`)
- 각 폴더에 목차 역할의 `README.md`를 두어 하위 문서를 나열한다.
- 문서 주제는 폴더로, 개별 항목은 파일명으로 구분한다.
- 파일명은 한글 표준명을 사용하고, 필요 시 영문/한자 병기 (`합곡(LI4).md`).
- **위키 각 제목(파일명/문서 제목)은 한글·영어·한자·외래어(원어)를 병기할 수 있다.** 외래어의 경우 해당 외래어의 원어를 함께 적는다. 예: `합곡(合谷, LI4)`, `보중익기탕(補中益氣湯)`.
- **동음이의/다의어로 식별이 필요한 문서는 동음이의 분류로 따로 처리한다.** 파일명에 구분자를 명시(예: `인삼(人蔘)` vs `인삼(삼, Panax ginseng)`의 모호함이 있는 경우)하거나, 필요 시 별도 분류 헤딩/괄호 표기를 사용해 명확히 구분한다. 폴더 내 `README.md` 목차에서 동음이의 분류 항목을 명시한다.
- **질환 문서는 KCD 코드(KCD-8, 한국표준질병사인분류 제8차 개정)에 근거하여 분류한다.** KCD 코드는 파일명이 아닌 본문(개요 첫 문단)에 명시한다.
- **본초·경혈·처방(방제) 이름은 한의과대학 교과서적인 표준 표기를 사용한다.** (예: 경혈 `합곡(LI4)`, 처방 `보중익기탕`, `소건중탕(小建中湯)`, `축천환(縮泉丸)`, 본초 `인삼`·`백출`) 지방/약어/비표준 표기를 지양하고, 최초 등장 시 한자·약리학적 표기를 병기한다. 표준 표기가 아닌 오기(예: 小建中湯을 "소견중탕"으로, 縮泉丸을 "수권환"으로 적는 것)는 사용하지 않는다.
- **한자 발음 표기 규칙 (강제)**: 한자를 병기할 때 해당 한자어의 읽기는 **반드시 표준 한국어 한자 발음(국어의 표준 한자음)**으로만 표기한다. 중국식 발음(병음·광동어)·일본식 발음·지방 발음·영어식 표기는 어떤 경우에도 사용하지 않는다. 예: 十味龍膽花 → **십미용담화**(十=십, 龍=용, 膽=담), 痰熱清 → **담열청**(痰=담), 咳喘平湯 → **해천평탕**(咳=해), 柴僕湯 → **시복탕**(僕=복), 金振口服液 → **금진구복액**(口服液=구복액), 加味玉屛風散 → **가미옥병풍산**(屛=병), 咳速寧顆粒 → **해속녕과립**(咳=해, 速=속, 寧=녕). 중국어 병음(예: Kesuning)을 그대로 한글로 옮긴 표기(예: 커수닝)는 금지한다.
- 각 폴더에 목차 역할의 `README.md`를 두어 폴더 내 문서를 나열한다.
- 파일 상단에 YAML front matter를 **넣지 않는다.** 대신 문서 첫 줄에 `# 제목` 헤딩과 함께 메타 정보(출처·작성일)는 본문에 자연스럽게 서술한다.
- 교차 참조는 상대 링크(`[요통](임상한의학/재활의학/요통.md)`)로 연결한다.
- **문서 표점 규칙 (강제)**: 문서 본문의 모든 문장 부호는 **한국식 표점**을 사용한다. 특히 중국어 표점인 `。`(모점)을 사용하지 않고 **반드시 `.`(마침표)로 통일**한다. AI 요약(`answer`/`clinical_summary`)이나 중국어 원문에서 `。`가 포함된 문장을 인용·번역할 때도 마침표 `.`로 변환하여 기재한다. 이 규칙은 문서 본문·각주 정의·표·Q&A 등 문서 전체에 적용한다.

## 위키 문서 구조

**문서 구조는 고정 템플릿이 아니라, 표제어의 성격·논문 근거에 맞춰 자유롭게 구성한다.** 아래 섹션 목록은 참고용이지 복사 대상이 아니다. 문서마다 다루는 내용(본초·경혈·처방·질환·용어)이 다르므로 목차·소제목·서술 순서도 그에 맞게 새로 짠다. 같은 틀의 문서가 반복 생산되면 임상 정보의 다양성과 깊이를 해치므로, 각 표제어의 근거와 임상 적용에 맞는 독자적인 구성을 만든다.

**다편(多篇) 구조 (강제)**: 단일 섹션 문서는 금지한다. 기준 문서처럼 **5편 이상의 대편(大編)**으로 구성한다 (예: 총론→병태생리→치법·방약·침구→KCD 질환군별 각론→예후·관리). 표제어 유형별 구조 가이드는 RUN.md §0-2를 참조한다.

**필수 공통 요소 (반드시 포함):**

- **다편 목차**: 표제어의 성격에 맞게 세분화된 소제목을 직접 설계한다 (예: 질환은 개요·병인병기·변증·진단·치료·예후·관리 등, 처방은 정의·구성·주치·변증·가감·주의사항 등). 표제어 유형별 가이드는 RUN.md §0-2 참조.
- **KCD 질환군별 각론 (질환·증후 표제어)**: KCD-8 코드별로 군(群)을 묶어 **6단 구성** (①코드 ②병인병기 ③병태생리 ④치료 ⑤예후 ⑥관리)으로 작성한다. 기준 문서의 제4편 구조를 따른다.
- **근거 각주**: 본문의 임상적 주장 문장 끝에 각주 `[^n]`를 달아 해당 근거 논문(제목·연구유형·저널·DOI/PMID 링크)을 연결한다. 연구유형은 한국어 라벨([메타분석]·[체계적 고찰]·[임상시험]·[관찰연구]·[증례 보고]·[실험연구]·[문헌 고찰] 등)로 표기한다(RUN.md §1-4-2-1).
- **각주 정의**: 문서 맨 끝에 **별도의 헤더 없이, 불릿 기호(`-`) 없이** 각주 마커(`[^n]:`)로만 나열한다. 참고문헌 섹션은 별도로 두지 않으며, 출처 정보는 각주 정의로 통일한다. 각주 정의는 **고정 포맷**(논문 제목·저널·출판일·[연구종류 한국어 라벨]·DOI/PMID 링크·`—` 부연 해석)을 따른다(RUN.md §1-4-2-1). 각주 정의에는 메타데이터와 함께 **근거의 의미를 한 줄로 부연 해석**을 덧붙인다(기준 문서 참조). 교과서·원전 이론은 `[교과서적 근거]` 표기로 각주 없이 서술한다.
- **근거 한계 명시**: 치료표·변증표·감별표 뒤에 "이 표는 임상 틀이지 동일 근거수준의 권고가 아니다" 식의 한계 문구를 부착한다.
- **변증 층화 강조**: "변증 없는 관행적 취혈/처방은 근거에 부합하지 않는다"는 원칙을 각 질환군 치료 섹션에 반복 명시한다.
- **감별진단**: 유사 변증 감별표 + 실무 순서 + 기질적 질환 배제 목록을 포함한다.
- **안전성 표**: 약물상호작용·부작용·임신 금기·기질질환 배제·정신과 공동 관리 등을 예후·관리편에 명시적 항목으로 둔다.
- **추적 지표표**: 질환군별 평가 지표(HAM-D, VAS, PSQI, 자궁동맥 PI 등)를 관리 섹션에 둔다.
- **환자 설명용 요약**: 관리편 말미에 일반인 향후 설명 단락을 둔다.
- **Q&A**: 임상 현장 자주 묻는 질문 5~8문항을 문서 끝(각주 정의 전)에 둔다.
- **조섭표**: 오로칠상·생활지도 표(수면·눈사용·활동·정서·식이·보온·방로 등)를 관리편에 둔다.
- **교차 참조**: 관련 변증·방제·본초·경혈 문서를 상대 링크로 연결한다 (본문 최초 등장 시 1회).
- **고전 인용 출처**: 『내경』·『상한론』·『금궤』 등 원전 출처를 문서 맨 끝에 명시한다.

참고용 섹션 예시(반드시 이 순서·이 이름을 따를 필요는 없음): `개요`, `근거 요약`, `임상 적용`, `경혈/처방 연관`(`method_specific_acupoint`, `method_specific_herbal*` 필드 활용), `Q&A`.

## 문서 생성·편집 워크플로우

상세 워크플로우는 RUN.md를 따른다. 핵심 원칙만 여기에 요약한다.

- **기준 문서 숙독 (강제)**: 작성 전 반드시 `간혈허`·`간기울결` 기준 문서 중 하나를 완독하고 동일한 수준의 문서를 만드는 것을 목표로 한다. 기준 문서의 필수 구조 요소는 RUN.md §0-1에 정리되어 있다.
- **점진적 증분 편집 원칙 (강제)**: 한 번에 전체를 재작성하지 않고 **부분적으로 점진적으로 보강**한다. 한 섹션씩 순차적으로 편집하고, 각 편집 후 `git diff --stat`로 변경 규모를 확인하며 진행한다. 대규모 문서(500줄 이상) 작성·보강 시 2단계 방식(목차 스켈레톤 → 헤더별 diff 채우기)을 따른다 (RUN.md §1-3).
- **새 표제어 주제 선정**: opencode는 기존 위키 문서 목록(`wiki/**`)을 참고하여 누락된 핵심 개념(본초·경혈·처방·질환·용어 등)을 **임의로 선정하거나**, 보강이 필요한 기존 문서를 스스로 선택하여 작업을 수행한다. 주제 선정 후 반드시 `glob`을 사용하여 전체 위키 디렉토리에서 해당 표제어가 포함된 모든 파일 목록을 확인하여 중복 생성을 방지한다. 특히 방제학/질환 문서에 등장한다고 해서 본초학/경혈학 문서가 없는 것이 아니므로, 분류 폴더별로 교차 검증하여 중복 생성을 완벽히 차단한다.
- 문서 주제 선정 → `GET /search`로 관련 논문 전수 수집 (`per_page=100`, 포괄적 검색어 확장, RUN.md §1-2)
- `analyzed=1`로 분석 완료된 논문만 우선 사용
- 논문 데이터와 `answer`/`clinical_summary`를 기반으로 문서 작성
- 각주 정의에 논문 메타데이터(DOI, PMID, 연구유형 한국어 라벨, 환자수)와 **근거 의미 부연 해석**을 반드시 포함. 형식은 RUN.md §1-4-2-1 고정 포맷 준수
- **파일 편집 후 반드시 `mkdocs.yml` 무결성을 확인한다.** 폴더·문서를 추가/삭제/이동하면 빌드 결과에 반영되는지 확인해야 한다. 편집 후 `python3 -m mkdocs build`로 무결성을 검증한다.

## 프로젝트 기술 스택

- **콘텐츠**: 순수 마크다운(`.md`) 위키 파일 (`wiki/`)
- **정적 사이트 생성기**: MkDocs + Material 테마 (`mkdocs.yml`)
- **자동화**: Python 스크립트 (`scripts/`) — 논문 검색(`search.py`), 품질 검증(`validate.py`)
- **데이터 소스**: [med.symbolicinfo.com](https://med.symbolicinfo.com) 검색 API

## 명령어

```bash
# 논문 검색 → 근거 수집
python3 scripts/search.py "요통" --km --human --analyzed

# 문서 품질 검증 (링크·KCD·표기)
python3 scripts/validate.py

# 정적 사이트 빌드
python3 -m mkdocs build

# 로컬 미리보기
python3 -m mkdocs serve
```

## 커밋 규칙

- **작업 완료 시 자동으로 커밋한다.** 사용자가 별도로 요청하지 않아도 작업이 끝나면 변경 사항을 커밋한다.
- 커밋 전 `git status`/`git diff`로 의도한 파일만 스테이징하고, 시크릿·불필요 파일은 제외한다.
- 커밋 메시지는 변경 내용을 간결히 요약한다 (예: `docs: 위키 분류 체계 초기화`, `feat: 논문 검색 스크립트 추가`).
- **커밋 후 `git push origin main`을 실행해 원격 저장소에 반영한다.**

## 자동화

`scripts/loop.py`가 opencode를 반복 호출해 RUN.md 워크플로우를 실행한다 (상세는 RUN.md §6).

```bash
python3 scripts/loop.py            # 무한 반복
python3 scripts/loop.py --max 20   # 20회만
```

## 주의사항

- API에 대한 과도한 요청은 피하고, 필요한 페이지만 요청 (`per_page` 최대 100).
- `q` 파라미터로 검색어를, `cat`/`km`/`human`/`lit`/`source`로 필터 조합하여 정확한 데이터를 얻을 것.
- 인용은 원문 데이터를 대조 후 작성하며, AI 요약을 그대로 인용으로 취급하지 않을 것.
- 비한의학 논문이 포함될 수 있으므로 `is_korean_medicine` 필드를 활용해 주제와 일치 여부를 확인할 것.
